# Cycle Audit 3
Type: CYCLE AUDIT
Status: CLOSED
Cycle / phase membership: cycle 3 — PH-7 (approved 2026-09-14), PH-8 (approved 2026-09-14; withdrawn and re-approved the same day), PH-9 (approved 2026-09-14); see `../phases/ROADMAP.md` ledger
Audited revision: `c9618c8` on `main` (the PH-9 approval commit; CI run 34858502090 success on both jobs)
Remediation revision: `7d6404f` (remediation) and `8c175fa` (its CI workflow fix — `7d6404f`'s run was rejected as a workflow file); gate, smoke, PostgreSQL suites and CI (run 34862165475, success on both jobs, including the new "came through the gate" step) in `../evidence/CYCLE-3-closure-verification.md`
Earlier in this cycle: the out-of-band audit `CYCLE-3-OOB.md` (CLOSED) examined PH-7 and PH-8 and recorded FND-0058..FND-0081; it did not reset the count (§6.4).
Method: INDEPENDENT — five reviewer agents in this runtime that did not author PH-9, each with the product context, read access to the tree and an isolated runtime (own API port 3041–3044, scratch database and upload directories, probe code outside the repository); the lead consolidated, deduplicated, challenged, remediated and re-verified. No area was reviewed only by the lead.
Opened: 2026-09-14 at ledger 3/3 (§6.4), in the commit that approved PH-9. Closed: 2026-09-14.

## Scope, methods and limits
| Area | Reviewer | Method | Runtime effects |
|---|---|---|---|
| Permission and security boundaries (staged uploads and linking, recovery limits and `SUPPORT_TRUST_PROXY`, `Retry-After`, note retry keys, system-message fields, strict contracts; rechecks of FND-0059/0068–0072) | independent agent (SEC) | code reading + live probes against its own API (port 3041, twice: trust proxy off and on); api unit and e2e suites re-executed | scratch only; servers stopped |
| Product correctness and evidence integrity (PH-9 against `PROJECT_CONTEXT.md`; provenance of the four subphase approvals and the phase approval; CI runs; screenshot sets; state documents vs Git) | independent agent (PRD) | `npm run verify` re-executed; per-commit test counts from the committed trees; `gh run view` of four runs; live API on port 3042; nine screenshots opened | scratch only |
| Architecture and reliability (`case-rules.ts`, supervision, `remind`, cleanup job and its races, migrations `0019`/`0020`, `RetryAfterFilter`, check-context rules, test quality) | independent agent (ARC) | code reading; drift check on a scratch copy of `apps/api`; upgrade from 0018 on a populated scratch PGlite; live API on port 3043; api unit suite | scratch only |
| Web client (split staff view and `runAction`, composers and keys, pending rows and sessionStorage, system-message wording, tabs, number fields, desktop close, the "Ao vivo" lag, test quality) | independent agent (WEB) | code reading against `41adb23`; four Vitest reproductions run from its scratch folder; a reproduction of Next's rewrite proxy; `npm test -w web` | one transient temp config written by Vite into the git-ignored `node_modules/.vite-temp/` through a scratch junction — gone when checked, `git status` clean throughout (recorded as an isolation limit) |
| Cold start and process (§8.5) | independent agent (COLD, fresh, read-only) | followed `CLAUDE.md` from disk with bounded reading (about 470 lines in 10 files); compared documents with Git, tags and CI; ran check-context; negative tests of the checker and the gate in a scratch clone | none in the tree |

Sampling rationale: PH-7 and PH-8 had been examined in depth by the out-of-band audit, whose security corrections were rechecked live; PH-9 (four subphases, two migrations, a new upload route shape, a new job, new checker rules) received the new depth; each reviewer spent live probes on the claims the evidence marked NOT VERIFIED or reasoned.

Not examined / limits on confidence: PostgreSQL concurrency was reasoned by the reviewers (no server was theirs to start); the lead's remediation added a PostgreSQL-only test of cleanup against a link in progress (`CYCLE-3-closure-verification.md`). No reviewer drove the web app in a browser (the build is bound to API port 3001); the lead's smoke and probes are the browser evidence. Screen readers and CSS layout were not examined. A real TLS proxy was not available (a scratch appending proxy stood in, PH-9.4). Gate provenance of past commits cannot be proven from history (FND-0098 — addressed going forward).

## Findings
| ID | Finding | Verdict | Severity | Rule | Disposition |
|---|---|---|---|---|---|
| FND-0082 | A public reply sent with a key the author already used for an internal note answered 201 with the note and stored no reply (SEC-1, PRD-F1 — found independently twice) | CONFIRMED | MINOR | RULE-SUP-03, DEC-0035 c | FIXED — the reply path refuses a key whose stored message is not public (409 `client_message_id_reused`), also on the unique-index fallback; unit test |
| FND-0083 | With `SUPPORT_TRUST_PROXY` on, one IPv6 /64 escaped the per-client limit (every address a new client) and could fill the instance ceiling for everyone (SEC-2) | CONFIRMED | MINOR | DEC-0038 b, §4.5 | FIXED — `clientBucket`: IPv6 counts by /64, IPv4-mapped addresses by their IPv4 address; unit test |
| FND-0084 | The "Aguardando equipe" queue view, its order and the summary's waiting age followed the status only, while supervision also counted open consultations — a case counted as waiting for a team was missing from the tab (PRD-F2, ARC-1 — found independently twice) | CONFIRMED | MINOR | DEC-0035 a, §5.2, §5.4 | FIXED — one rule: `waitingInternalSinceSql` (SQL twin, agreement test on every row) drives the view and its order; summaries carry `waitingInternalSince(row, oldest open consultation)`; the queue label no longer depends on the status |
| FND-0085 | The cleanup removed at most 200 uploads per hourly run (about 4 800 a day, below what uploads can produce) (ARC-2) | CONFIRMED | MINOR | DEC-0038 c | FIXED — batches until one comes back short, at most 50 per run (10 000); unit test |
| FND-0086 | The guards between cleanup and linking (still-unlinked delete, update-count checks) had no test; removing one kept every suite green (ARC-3) | CONFIRMED | MINOR | DEC-0038 c | FIXED — service test (a removed file fails the send and the creation, no case left); PostgreSQL-only test of a cleanup meeting a link in progress |
| FND-0087 | The staff composer reused its retry key for an edited message after a failure; a first attempt stored without an answer would make the edited one vanish while reported sent (WEB-1) | CONFIRMED | MINOR | RULE-SUP-03, DEC-0036 b | FIXED — a key is reused only for the same content (mode, text, files); tests for an edited retry and a same-text retry. Server-side body comparison: not added (keys are the client's retry contract) |
| FND-0088 | Sending while a file was still uploading dropped the file silently (new request; also the conversation and staff composers) (WEB-2) | CONFIRMED | MINOR | §10.1, §10.2 | FIXED — the composer reports running uploads; the three forms wait; tests |
| FND-0089 | "Ao vivo" stayed on for the whole first backoff after a stream ended (WEB-3; the PH-9.2 observation) | CONFIRMED | MINOR | §10.1 | FIXED — "reconnecting" is reported as soon as the connection is lost; test with fake timers. A silent network drop still waits for the stale-stream watchdog (documented) |
| FND-0090 | Closing the panel on desktop left the mobile flag up, so crossing the breakpoint reopened it (WEB-4) | CONFIRMED | MINOR | DEC-0037 b | FIXED — closing closes both layouts; test with a controllable `matchMedia` |
| FND-0091 | Test gaps: the mobile shell branch untested since PH-9.3 (jsdom has no `matchMedia`), per-mode keys not pinned, upload-in-progress untested (WEB-5) | CONFIRMED | MINOR | §7.3 | FIXED — `ShellLayouts.test.tsx` (mobile toggle, breakpoint), key-per-mode test, upload tests; the tab panel is focusable when empty |
| FND-0092 | A notice with missing data rendered "Continuação do caso ."; the weekday check accepted inherited keys (WEB-6) | CONFIRMED | MINOR | RULE-SUP-07 | FIXED — missing value → the stored body; own-property check; test |
| FND-0093 | A new request whose staged file was gone got "check your connection" and could never succeed on retry (PRD-F6) | PARTIAL | MINOR | RULE-SUP-07 | FIXED — `attachment_not_available` gets its own copy and clears the files so the next send can succeed; test |
| FND-0094 | Evidence ran ahead of itself: the phase record said the smoke covered every PH-1..PH-9 journey (the two PH-9.3 journeys were only in a probe); screenshot `ph-9.2/07` does not show its claim; gate results recorded as "at commit time" without counts; CI stated before its run (PRD-F3/F4/F5) | CONFIRMED | MINOR | §7.1, §9.2 | FIXED — dated corrections in the five PH-9 records; the smoke now observes both PH-9.3 journeys (`screenshots/cycle-3/`); gate counts travel in the commit (FND-0098) |
| FND-0095 | Stale or undiscoverable context: FEAT-ACCESS said both 30 and 120 for the ceiling; DEC-0028 carried the old limit without an amendment note; feature "Decisions" sections missed DEC-0029/0034–0038; the index missed `access/`, `common/` and `access.ts` and listed a release record twice; the decision log ran in two directions (COLD-1/2/5) | CONFIRMED | MINOR | §3.1, §5.3 | FIXED — contexts, DEC-0028 status note, index rows; the decision log sorted oldest first |
| FND-0096 | The gate's "tree unchanged during verify" hashed paths and states only; a second edit to an already-modified file went unnoticed (COLD-3) | CONFIRMED | MINOR | §7.4, §9.1 | FIXED — the hash covers `git diff HEAD --binary` and the named new files; demonstrated on a disposable worktree |
| FND-0097 | The `c9618c8` commit message describes the discarded trust-proxy default ("default 1") (COLD-4) | CONFIRMED | MINOR | §7.1 | DOCUMENTED — history is not rewritten; corrected in `PH-9-phase-approval.md` and here |
| FND-0098 | Nothing in a commit showed it came through the gate (COLD-6) | INCONCLUSIVE for past commits | MINOR | §9.2, BL-020 | FIXED going forward — the gate adds a `Gate-Verified:` trailer with the suites' counts and CI refuses a pushed commit without it |
| FND-0099 | Process cost and naming: phase-closing commits touch 16 documents and about 4 MB of screenshots; the out-of-band record titled itself "Cycle Audit 3" (COLD-7) | CONFIRMED | MINOR | §8.5 | PARTLY FIXED — the out-of-band record retitled. Process cost: accepted and measured again at the next audit (the screenshot rule DEC-0034 c is the containment) |
| FND-0100 | Ledger record parsing: a Markdown-link record cell or an out-of-band record cited first would give a false verdict; a lowercase `-oob.md` escaped the rule (ARC observation) | CONFIRMED | MINOR | §6.4 | FIXED — every `.md` path of the cell is read, the first non-out-of-band one is the record, the rule is case-insensitive |

Rechecked and holding: the out-of-band security corrections (FND-0059 test-database guard, FND-0068/0069 limiter, FND-0070 contact normalization, FND-0071 control and bidi characters, FND-0072 bind address); FND-0081 (the entrypoint still warns that an injected copy may be stale; today's copy matched the disk). "An out-of-band audit does not reset the count" and "audit due pauses feature work" are stated and enforced (COLD, negative tests in its clone).

## Closure
Closed on 2026-09-14 (§8.4): every required area was examined by an independent reviewer with its limits recorded; no finding was CRITICAL or MATERIAL; seventeen findings are fixed with tests or corrected records, FND-0097 is documented (history is not rewritten), FND-0098 holds from `8c175fa` on, and FND-0099's process cost is accepted and measured again at the next audit. The remediation candidate `8c175fa` passed the gate (with its `Gate-Verified` trailer), the browser smoke (56 observations), the api suites on PostgreSQL 16 and CI run 34862165475 on both jobs. The ledger resets through this record: cycle 3 CLOSED, cycle 4 opens at 0/3.
