# Closing audit (cycle 4, out of band at the project's closure)
Type: CYCLE AUDIT
Status: CLOSED
Cycle / phase membership: cycle 4 at 1/3 (PH-10 approved 2026-09-14); PH-12 (project closure) in progress — this audit is PH-12.2; requested by the Owner's closure instruction («finalizar todas las cosas pendientes para cerrar el proyecto», 2026-09-14). Out of band: it does not reset the ledger (§6.4).
Audited revision: `33e8794` on `main` (the PH-12.1 commit; CI run 34871371921 success on both jobs)
Scope: PH-10 (operating policies in the product — schedule and time zone, formal complaints, exports, recovery to Verification) and PH-12.1 (secret masking, e-mail dead letters, checker rules, the removed deploy CLI); rechecks of Cycle Audit 3's corrections; the hand-over (`../phases/PH-11.md`, `../phases/PH-12.md`) and the state documents.
Method: INDEPENDENT — five reviewer agents in this runtime that did not author PH-10..PH-12, each with the product context, read access to the tree and an isolated runtime (own API port 3041–3044 from a scratch build of the audited commit, scratch database and upload directories, probe code outside the repository); the lead consolidated, deduplicated, challenged, remediated and re-verified.
Opened: 2026-09-14. Closed: 2026-09-14 on the remediation commit (child of `33e8794`; evidence `../evidence/CLOSING-verification.md`).

## Scope, methods and limits
| Area | Reviewer | Examined | Method | Not examined / limits |
|---|---|---|---|---|
| Security boundaries | SEC | Exports (all four roles + anonymous; content vs. the customer's own screens; staged and unlinked uploads; unknown ids), every staff route against a complaint, 26 masking inputs, dead letters, availability/settings edge values, `SUPPORT_TRUST_PROXY` on and off, FND-0082/0083/0086 rechecks | Live HTTP probes against a scratch `tsc` build of `33e8794` (the committed `dist` predated the commit), `npm test -w api` | Web UI, PostgreSQL server path, live SSE, retention; the dead-letter path verified by unit test and source (the simulated notifier cannot fail) |
| Product correctness and evidence | PRD | Every DEC-0039 policy traced to code and, where the dist allowed, reproduced live; PH-10.x/PH-12.1 plans and evidence vs. Git, test counts, CI verdicts, five cited screenshots; the PH-11 brief | `npm run verify` (exit 0 first run), `npm audit`, `gh run view`, live flows on port 3042 | Browser; `test:pg`; the checker's worktree negative; screenshots 02–24/26–31; masking and dead letters live (the dist lacked PH-12.1) |
| Architecture and reliability | ARC | `settings.ts` across DST gaps/overlaps, 23/25-hour days, boundaries, "24:00", performance; every transaction block for nested queries (17 blocks linted); complaint gates; export cost (300 cases); migrations 0021–0023 inside the migrator's transaction; dead-letter races; `maskLikelySecrets` lookbehind vs. Next's browser floor and a 1000-char worst case; the checker's path rule; test quality | Standalone probes against the shared dist, live probe on port 3043, read-only suites | PostgreSQL pool concurrency and `ADD VALUE` reasoned, not run; web bundle not rebuilt |
| Web client | WEB | Every web file of the audited range (20); availability under Europe/Lisbon and Asia/Tokyo through a scratch vitest config; settings switch; complaints in queue/case/supervision; export download and error paths; recovery note; the new tests' strength | `npm test -w web` (105 baseline), scratch reproductions (5 tests) | No browser run; download blocking, `revokeObjectURL` timing, Safari/Firefox reasoned; screen readers and layout |
| Cold start and process | COLD | The `CLAUDE.md` route from disk (about 400 lines before the first correct answer), FEAT-STAFF complaints traced doc → code; the hand-over test of `PH-11.md` (every named file and symbol verified — none wrong); state documents vs. Git; gate discipline (`git log --format='%h %(trailers:key=Gate-Verified)'` — every commit since `7d6404f` carries the trailer); process cost (files per commit 6/26/42/74/30) | Read-only `git`, `gh`, `npm run check:context` | No suites or servers; GOVERNANCE read selectively |

Independence: none of the reviewers wrote PH-10..PH-12; the lead is the author of the audited work and of this consolidation, and states so (§8.5 — not a human review).

## Findings
Numbered from FND-0101. Severity per §8; verdict after the lead's challenge. "Fixed in" is the remediation commit unless stated.

| ID | Title | Severity | Verdict | Source | Remediation |
|---|---|---|---|---|---|
| FND-0101 | A customer's export carried the whole event history unfiltered — assignments with staff ids, priority changes, consultation requests/answers with team and answerer, incident links with the incident title — none of which the customer's own screens show (DEC-0042 "what the customer can see", RULE-SUP-01/-04); the e2e test only asserted `events.length > 0` | MATERIAL | CONFIRMED (live: one worked case exported through `POST /api/staff/customers/:id/export` as the admin) | SEC | `exportCase` projects `CUSTOMER_VISIBLE_EVENT_TYPES` (`case_created`, `status_changed`, `case_resolved`, `case_reopened`, `case_closed`, `follow_up_created`, `reminder_sent`) as `CustomerCaseEvent` without `actorId`; the e2e test builds a case with a take, a priority change, a consultation and an incident and asserts the export holds exactly the visible types and none of the internal strings or staff ids; DEC-0044 a |
| FND-0102 | The customer-facing note under "Reclamação formal" announced "prazo interno de 5 dias úteis para a resposta" while DEC-0041, `PH-10.2.md` ("out of scope: a customer-facing promise of the deadline") and the PH-10 approval record said the deadline is internal and not announced; the smoke recorded it as a feature; RULE-SUP-08 (no promised response time) | MATERIAL | CONFIRMED | WEB, PRD | Copy reduced to the supervisor; the form test asserts no "dias"/"prazo"; the smoke note corrected; `PH-10.2.md`'s contradictory line corrected; DEC-0044 e |
| FND-0103 | `ROADMAP.md` "Active chain" said both "cycle 4 is at 1/3" and "at 0/3", both "PH-1..PH-10 APPROVED" and "PH-1..PH-9 APPROVED", and that PH-11 "follows, pending the provider account" next to "the Owner will build it directly with GPT" — append-only prose the checker does not read (the ledger table was right) | MATERIAL | CONFIRMED | COLD, PRD | The paragraph rewritten as the current chain only; history stays in the ledger and the audit records |
| FND-0104 | The PH-11 brief named every file and symbol correctly but omitted what an outside builder would break: the case-domain invariants (DEC-0015/16/17, `mutate`), the SQL twin of `awaitingReplySince` and the queue ordering, the three consumers of the shared `actor_type` enum, the `assistant_state` projection decision, the notification kind and the pt-BR dictionary, the untrusted-input rule, the public/internal split of knowledge (§12), the third-party data transfer as an Owner decision, the home copy "Atendimento feito por pessoas", and how to open a phase under this governance (status flips, subphase docs, the gate, the ledger reaching 3/3) | MATERIAL | CONFIRMED | COLD, PRD | `PH-11.md` gains "Repository rules the build must follow" and "How to start"; three Owner decisions added; `PH-12.md` says the brief is `PH-11.md` itself |
| FND-0105 | The closure plan said nothing about who discharges Cycle Audit 4 when PH-11 is approved (§6.4: a pause does not erase audit debt), who keeps the state documents after closure, or that `CLAUDE.md`'s "subagents are available in this runtime" is this agent's runtime, not the repository's; no root `AGENTS.md` pointer for other providers (§0.2) | MATERIAL | CONFIRMED | COLD | `PH-12.md` "After closure" (audit obligation, process owner); `CLAUDE.md` wording; root `AGENTS.md` that only points to `CLAUDE.md` |
| FND-0106 | Around `assertMayWorkComplaint`: `POST /api/staff/incidents/:id/notes` and `/resolve` wrote agent-authored internal notes into linked complaints (`noteLinkedOpenCases` has no check), `POST /:id/read` took no actor, `POST /:id/attachments` staged files on a complaint, and `takeCase` checked before the lock only | MINOR | CONFIRMED (live by SEC and PRD) | SEC, PRD, ARC | Incident-wide notes skip complaints for someone who may not work them (`{ delivered, skippedComplaints }`), `markStaffRead(staff, id)` checks, the upload route checks (`assertStaffMayWork`), `takeCase` rechecks under the lock; unit and e2e tests; DEC-0044 b |
| FND-0107 | Reclassifying an agent-owned case into a complaint left it assigned to the agent, who could then neither work nor release it (403 on reply, release and transfer) — the state DEC-0041 forbids, reachable only this way; `assertComplaintTarget` was not rechecked under the lock; the web offered agents as transfer targets and worded the 400 as a generic failure | MINOR | CONFIRMED (live: agent takes → supervisor reclassifies → `assignedAgentId` unchanged) | SEC, ARC, WEB | `updateAttributes` releases an owner who may not work complaints in the same `mutate` (`case_assigned`, `released: true`, `reason: formal_complaint`; a conflicting assignee change → 409); `assignCase` rechecks the target under the lock; the web filters transfer/reassign targets with `staffMayWorkComplaint` and maps `supervisor_required` to the rule's copy; tests; DEC-0044 b |
| FND-0108 | Masking under-coverage: Spanish `clave`, Portuguese `chave`/`palavra-passe`, `passcode`, plurals, digits spelled with spaces or dashes, all-caps codes, and a real seed phrase (twelve lowercase words — only the first token after "frase de recuperação"/"seed" was examined, so the keywords implied a coverage that did not exist) | MINOR | PARTIAL (the original never leaks through another route; the documented heuristic limit stands) | SEC, PRD, ARC | Keywords widened (with "de acesso/verificação/segurança" between keyword and token), `SPACED_CODE`, all-caps rule, phrase keywords mask the rest of the sentence when it reads like a word list (≥ 8 words, ≤ 1 word of prose); tests for each and for prose about a lost phrase; DEC-0044 c |
| FND-0109 | Dead letters: the claim predicate ignored `email_failed_at` (a list read before parking could claim a parked row), `dead` was decided from the in-memory count while SQL incremented the column, `error.stack` was logged verbatim (a real provider's message could quote the recipient), nothing documented how a dead letter is found or re-queued; all moot under DEC-0033's single instance | MINOR | CONFIRMED (code) | ARC, SEC | Claim requires `email_failed_at IS NULL`; one `UPDATE … RETURNING` decides attempts and parking from the stored column; the log carries the error's name and a bounded message; `OPERATIONS.md` says how to re-queue; DEC-0044 d |
| FND-0110 | `check-context` used `existsSync`, case-insensitive on Windows: `docs/Phases/roadmap.md` passed the local gate and would fail in CI (§9.2 parity) | MINOR | CONFIRMED (reproduced) | ARC | `existsExact` compares the requested spelling with `realpathSync.native`; negative exercised in a disposable worktree; DEC-0044 f |
| FND-0111 | The home said "Hoje: …" for the operation's day even when the customer's calendar day differed (Lisbon at 02:00 read "Hoje: 13:00–22:00" for a closed Tuesday; Tokyo read a window that had already ended); bounded to narrowed schedules (24/7 announces nothing) | MINOR | CONFIRMED (scratch vitest under forced zones) | WEB | "Hoje" only when the window opens on the customer's own day, else the weekday is named; a day open around the clock says until when; a window already past is omitted; `SupportHome.zone.test.tsx` runs under Asia/Tokyo with literal strings; DEC-0044 e |
| FND-0112 | Export section: "O arquivo foi baixado" asserted a download the page cannot observe and left no way to save the file again without a new recorded export; every non-403 error blamed the ID/reason; a failed list read as "none recorded"; the object URL was revoked synchronously | MINOR | CONFIRMED (jsdom) | WEB | "O download foi iniciado", "Baixar de novo" reusing the last export (no new record), 400 → input copy, other errors → "tente de novo", list failure → its own line with a retry, revoke after 60 s; test; DEC-0044 e |
| FND-0113 | Resolved and closed complaints showed "Prazo vencido há N d" in attention styling; the case view never said overdue | MINOR | CONFIRMED (reproduced) | WEB | One `complaintDeadlineLine(status, deadline)` for queue, case and supervision: overdue only while open, "Prazo era …" once finished; test |
| FND-0114 | Evidence overstated: PH-12.1 claim 5 said the web test covers the masked-description note (no test referenced it; the fixture was dead code); PH-10.1 said a different-zone rendering "is established by the unit tests" while those tests computed expectations with the implementation's own formatter | MINOR | CONFIRMED | WEB | Both evidence lines corrected with dated notes (§7.3: the record says what was and was not verified); the masked-note test and the forced-zone test now exist |
| FND-0115 | Stale statements: `CONTEXT_INDEX.md` listed `CYCLE-3.md` as open; `CURRENT_STATE.md` line 4 named the PH-10 checkpoint while the handoff named PH-12.1's; BL-031 still said "Agent: PH-11 plan"; `OPERATIONS.md` said "PH-10 brings the rest in", "Not built — PH-11" without the GPT note, and that customers see "whether it is still a working default" (removed in PH-10.1); FEAT-ACCESS quoted the old button label; FEAT-STAFF "Where to work" did not list `SupervisionPanel.tsx`/`DataExportSection.tsx`; DEC-0040 was cited from FEAT-CHAT only | MINOR | CONFIRMED | COLD, PRD | All corrected; DEC-0040/0041/0042/0044 cited from every context they affect |
| FND-0116 | DEC-0042 says the export contains "what the customer can see", but the simulated e-mail outbox shown on the home is not in it (the hint copy lists the specifics honestly) | MINOR | PARTIAL | PRD | Disposition: the outbox is a simulation artifact of the missing provider, not the customer's support data — DEC-0042 amended to say so (DEC-0044 a); no code change |
| FND-0117 | Guard limits: CI only greps the `Gate-Verified` trailer string (a discipline signal, not authentication — known since FND-0098 but not stated in the runbook); the gate's hash does not cover git-ignored inputs verify may read; the checker sees paths, not prose facts (FND-0103/0115 all passed it), bare filenames, symbol names or evidence freshness; `git commit` inside the gate re-runs `check:context` through the hook (cost only) | MINOR | CONFIRMED | COLD | Accepted and recorded in `../runbooks/VERIFICATION.md` (DEC-0044 g); no code change — the corroboration is CI (§9.2) |

Rechecks with no finding: FND-0082 (reply/note keys → 409 both ways, same-mode retries 201), FND-0083 (IPv6 /64 buckets, zone ids and `::ffff:` mapping, forged left-hand XFF entries mint nothing), FND-0086 (cleanup vs. linking guards and the PostgreSQL-only test present); `SUPPORT_TRUST_PROXY` default off ignores `X-Forwarded-For`; settings reject `open: "24:00"`, `close: "24:01"` and `00:00–00:00`; the customer never receives `complaintDeadlineAt` on any surface; `businessDaysAfter` and `zonedInstant` correct across DST gaps and overlaps, 23/25-hour days and year boundaries; no nested query inside any of the 17 transaction blocks; the migrator's single transaction is legal for `ADD VALUE` on PostgreSQL 16 (a later migration using the value in the same batch would fail on an upgraded database — noted); `maskLikelySecrets` lookbehind is inside Next 16.3.5's browser floor (Safari 16.4) and takes 1.2 ms on a 1000-char worst case; test counts and CI verdicts reproduce exactly; every commit since `7d6404f` carries the gate trailer.

Observations without a finding: the settings "Dia inteiro" switch discards typed hours when toggled off (UX); a partial day ending at "24:00" is unreachable from the form but valid for the schema; `positiveNumberEnv` accepts "1e3"/"0x10" and treats "0" as the default (harmless); the PH-10.2 supervisor test is thin (asserts "Assumir caso" and the absence of the lock only); the complaint deadline unit assertion "5–9 days" would also accept a flat +7 (the live probe confirmed five business days).

## Closure
All seventeen findings remediated or dispositioned on the remediation commit; evidence `../evidence/CLOSING-verification.md` (suites, checker negative, CI). No MATERIAL finding stays open. The ledger stays at cycle 4 = 1/3 (out of band; PH-12's approval in PH-12.3 makes it 2/3). Closed 2026-09-14 by the Agent (§8.6; not a human review).
