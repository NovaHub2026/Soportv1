# BACKLOG
Type: BACKLOG — repository fallback (DEC-0001)
Updated: 2026-09-14

| ID | Type | Item | Impact / why | Owner / next step | Status |
|---|---|---|---|---|---|
| BL-001 | Decision | Access to Orbit's code, API or environment for real identity/record integration | Resolved 2026-09-13: the Owner confirmed no Orbit repository or API exists (DEC-0003). Not a blocker — a fact. All Orbit context is simulated behind the ADR-0002 boundary. Reopen as integration work when an Orbit system exists | Owner informs when Orbit exists; Agent keeps the boundary narrow | RESOLVED |
| BL-002 | Operational unknown | Staffing, hours, response targets, roles/specialist contacts, account-recovery procedure, retention/complaints policy (context §13.2) | Needed before honest production claims (RULE-SUP-08) and PH-8 | Owner/Operations; not blocking PH-1..7 | OPEN |
| BL-003 | Process | Automated state/link consistency check for context documents | Guards documentation drift as the repo grows (§7.5) | Done in PH-1.1: `../scripts/check-context.mjs`; control status PARTIAL (see BL-004) | DONE |
| BL-004 | Process | Extend `check-context` with an audit-debt check (first-time phase approvals in the ledger vs cadence) | State-integrity control becomes relevant at the first phase approval (§6.4, §16.7) | Done in PH-1.5: ledger parsing, `n/3` consistency with CURRENT_STATE, failure when 3 approvals have no audit record; negatives exercised | DONE |
| BL-005 | Tech debt | TypeScript 6 in `apps/api` vs TypeScript 5 in `apps/web` | Two compiler majors in one workspace; harmless now, confusing later | Agent; align when Next/eslint-config-next support TS 6 | OPEN (low) |
| BL-006 | Tech debt | npm 11 `allow-scripts` warning: `unrs-resolver` postinstall not allowed | ESLint import resolution passes today; may break on upgrade | Agent; approve the script (`npm approve-scripts`) only if lint breaks | OPEN (low) |
| BL-007 | Environment | Playwright Chromium lacks system libraries on the Owner's WSL2 (`libnspr4`, `libnss3`, `libasound2`); no passwordless sudo | UI smoke on WSL2 needs the `LD_LIBRARY_PATH` workaround (runbook). Not needed on the Windows host used since 2026-09-14 | Owner: run `sudo npx playwright install-deps chromium` once on WSL2 | OPEN (low, WSL2 only) |
| BL-008 | Process | Add the UI smoke (or a Playwright test suite) to CI once it runs in a clean container | Browser evidence is produced locally only | Agent; consider at PH-2 when the smoke grows | PLANNED |
| BL-009 | Product/ops | Clean up attachments uploaded but never linked to a message (orphans) after a grace period | Disk growth; not a correctness issue (orphans are only readable by their uploader) | Agent; with the operational jobs in PH-6/PH-8 | PLANNED |
| BL-010 | Product | Allow attachments on the very first message of a new case (needs a draft case or a two-step create) | Customers attach right after creating the case today | Agent; revisit with PH-4 contextual entry (record cards may reduce the need) | PLANNED |
| BL-011 | Product (audit FND-0023) | Staff views for waiting, resolved and closed cases, overdue-follow-up signal, pagination and a `(status, resolved_at)` index | §14.9 only partly demonstrated at PH-3 | Done in PH-5.1 (views, "Sem resposta há …", pagination, indexes `0009`); the configurable overdue threshold and the supervisor overview land in PH-5.4 | DONE (PH-5.1) / overdue threshold → PH-5.4 |
| BL-012 | Security/ops (audit FND-0018/0025) | Production hardening: per-identity SSE cap, upload rate limit/quota, `helmet` / no `X-Powered-By`, JSON 404s, bind address and unset-`NODE_ENV` policy for the simulated provider | Availability and disclosure risks exist only once deployed (nothing is deployed) | Agent; PH-8 (blocking condition for any release) | PLANNED |
| BL-013 | Product (audit FND-0024) | Idempotency key for internal notes; render attachments on pending/failed rows; keep composer chips until the send succeeds | Minor RULE-SUP-03/-07 gaps in the staff and customer composers | Agent; next touch of the composers (PH-5 at the latest) | PLANNED |
| BL-014 | Tech debt (audit FND-0024) | Extract a `runAction` helper and split `StaffCaseView.tsx` (787 lines, ten near-identical handlers); tests for the workspace picker and hidden-tab marking | Maintainability; no product defect | Agent; PH-5 when the workspace grows | PLANNED |
| BL-015 | UX (audit FND-0024) | Desktop "Fechar suporte" / "×" do nothing: either hide the panel on desktop when closed or remove the controls there | Honest controls (§10.1) | Agent; with PH-4 contextual entry (panel placement is decided then) | PLANNED |
| BL-016 | Decision (audit FND-0026) | Role model: may a non-owner agent set status, resolve, close or open a consultation on a colleague's case? Attribution is preserved today | RULE-SUP-02/-09 design question | Agent prepares a recommendation for PH-7 (roles/permissions) | OPEN |
| BL-017 | Tech debt (audit FND-0027) | Dev-only advisories: `drizzle-kit` → `@esbuild-kit/*` / esbuild, `@nestjs/mau` → undici/tmp/inquirer | Not shipped; noise in `npm audit` | Agent; on the next toolchain upgrade (BL-005) | OPEN (low) |
| BL-018 | Tech debt (audit FND-0027) | Shared response schemas (`caseSummarySchema`, `staffCaseDetailSchema`, …) are exported but unused: use them to validate API responses in e2e, or prune | Contract drift would go unnoticed | Agent; with the next contract change | OPEN (low) |
| BL-020 | Process (FND-0028) | Commit `3217c53` was created after `npm run verify` failed at `check-context` (shell chain used `;`) and carried unfinished files of a later block; CI red for one commit, corrected by the next | Gate discipline (§7.2, §9.2) | Agent: always `verify && commit`; only finished blocks in the tree at commit time; consider a git pre-commit hook running `check:context` (PH-8) | OPEN (rule adopted; hook pending) |
| BL-019 | Verification (audit FND-0009) | Run the api unit and e2e suites against a server PostgreSQL (service container) to confirm the row-lock semantics of `mutate()` under a real pool | Concurrency fixes are verified on PGlite's serialized connection only | Agent; PH-8 (blocking condition for any release); ADR-0003 revisit | PLANNED |
