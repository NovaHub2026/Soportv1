# BACKLOG
Type: BACKLOG — repository fallback (DEC-0001)
Updated: 2026-09-14

| ID | Type | Item | Impact / why | Owner / next step | Status |
|---|---|---|---|---|---|
| BL-001 | Decision | Access to Orbit's code, API or environment for real identity/record integration | Decided 2026-09-13: Orbit does not exist yet (DEC-0003) — there is no real identity or records adapter to build, so everything runs behind the labeled simulation (ADR-0002). Wording clarified by the cycle 3 audit (FND-0063): "resolved" meant "decided", not "available" | 2026-09-14 (DEC-0045): the broker exists — its frontend was analysed read-only and the `optaqode` adapters are prepared behind the ports (PH-13.1, `integration/ORBIT-INTEGRATION.md`). Owner: backend access and the answers of BL-032 for the live connection (PH-13.3); until then a production release is impossible (`runbooks/RELEASE.md` #4) | PREPARED (adapters, not connected) — blocks any production release |
| BL-002 | Operational unknown | Staffing, hours, response targets, roles/specialist contacts, account-recovery procedure, retention/complaints policy (context §13.2) | Needed before honest production claims (RULE-SUP-08) and a release | Decided by the Owner on 2026-09-14 (DEC-0039) and in the product since PH-10 (schedule 24/7 in the customer's zone, formal complaints, admin exports, recovery to Verification). Open: retention of closed cases (pending legal advice) and the e-mail provider (later) — blocking condition #5 of `runbooks/RELEASE.md` stays open for retention | PARTLY DECIDED (retention, e-mail provider open) |
| BL-003 | Process | Automated state/link consistency check for context documents | Guards documentation drift as the repo grows (§7.5) | Done in PH-1.1: `../scripts/check-context.mjs`; control status PARTIAL (see BL-004) | DONE |
| BL-004 | Process | Extend `check-context` with an audit-debt check (first-time phase approvals in the ledger vs cadence) | State-integrity control becomes relevant at the first phase approval (§6.4, §16.7) | Done in PH-1.5: ledger parsing, `n/3` consistency with CURRENT_STATE, failure when 3 approvals have no audit record; negatives exercised | DONE |
| BL-005 | Tech debt | TypeScript 6 in `apps/api` vs TypeScript 5 in `apps/web` | Two compiler majors in one workspace; harmless now, confusing later | Agent; align when Next/eslint-config-next support TS 6 | OPEN (low) |
| BL-006 | Tech debt | npm 11 `allow-scripts` warning: `unrs-resolver` postinstall not allowed | ESLint import resolution passes today; may break on upgrade | Agent; approve the script (`npm approve-scripts`) only if lint breaks | OPEN (low) |
| BL-007 | Environment | Playwright Chromium lacks system libraries on the Owner's WSL2 (`libnspr4`, `libnss3`, `libasound2`); no passwordless sudo | UI smoke on WSL2 needs the `LD_LIBRARY_PATH` workaround (runbook). Not needed on the Windows host used since 2026-09-14 | Owner: run `sudo npx playwright install-deps chromium` once on WSL2 | OPEN (low, WSL2 only) |
| BL-008 | Process | Add the UI smoke (or a Playwright test suite) to CI once it runs in a clean container | Browser evidence is produced locally only | Decided 2026-09-14 (DEC-0027 e): the smoke stays a local phase- and release-candidate control; CI builds the apps. Revisit event: the first hosted deployment (not reached; the PH-8 revisit did not happen — FND-0065) | DEFERRED (revisit: first hosted deployment) |
| BL-009 | Product/ops | Clean up attachments uploaded but never linked to a message (orphans) after a grace period | Disk growth; not a correctness issue (orphans are only readable by their uploader) | Done in PH-9.4 (DEC-0038 c): `AttachmentCleanupJob` hourly, grace `SUPPORT_UNLINKED_ATTACHMENT_GRACE_HOURS` (24 h), staged uploads included; linking checks the update count | DONE 2026-09-14 |
| BL-010 | Product | Allow attachments on the very first message of a new case (needs a draft case or a two-step create) | Customers attach right after creating the case today | Done in PH-9.3 (DEC-0037 a): staged uploads (`POST /api/support/attachments`) linked to the first message by `createCase`; the new-request form shows the composer. The follow-up form of a closed case stays text-only | DONE 2026-09-14 |
| BL-011 | Product (audit FND-0023) | Staff views for waiting, resolved and closed cases, overdue-follow-up signal, pagination and a `(status, resolved_at)` index | §14.9 only partly demonstrated at PH-3 | Done in PH-5.1 (views, "Sem resposta há …", pagination, indexes `0009`); the configurable overdue threshold and the supervisor overview land in PH-5.4 | DONE (PH-5.1) / overdue threshold → PH-5.4 |
| BL-012 | Security/ops (audit FND-0018/0025) | Production hardening: per-identity SSE cap, upload rate limit/quota, `helmet` / no `X-Powered-By`, JSON 404s, bind address and unset-`NODE_ENV` policy for the simulated provider | Availability and disclosure risks exist only once deployed (nothing is deployed) | Done in PH-8.1 (DEC-0031): SSE cap, upload limit, helmet headers, no fingerprint, JSON 404, loopback bind + warning; the unset-`NODE_ENV` policy is the warning plus the release runbook (PH-8.3) | DONE 2026-09-14 |
| BL-013 | Product (audit FND-0024) | Idempotency key for internal notes; render attachments on pending/failed rows; keep composer chips until the send succeeds | Minor RULE-SUP-03/-07 gaps in the staff and customer composers | Done: API in PH-9.1 (`postNoteSchema.clientMessageId`, DEC-0035 c); composers in PH-9.2 (per-mode retry keys, file names on pending/failed customer rows — DEC-0036 b) | DONE 2026-09-14 |
| BL-014 | Tech debt (audit FND-0024) | Extract a `runAction` helper and split `StaffCaseView.tsx` (787 lines, ten near-identical handlers); tests for the workspace picker and hidden-tab marking | Maintainability; no product defect | Done in PH-9.2 (DEC-0036 e): `runAction` in `StaffCaseView.tsx`, split into `CaseActions`, `StaffComposer`, `StaffMessage`, `CaseContext`; tests for the workspace picker and hidden-tab marking (`apps/web/src/features/staff/WorkspaceDebt.test.tsx`) | DONE 2026-09-14 |
| BL-015 | UX (audit FND-0024) | Desktop "Fechar suporte" / "×" do nothing: either hide the panel on desktop when closed or remove the controls there | Honest controls (§10.1) | Done in PH-9.3 (DEC-0037 b): on desktop "×" and "Fechar suporte" hide the side panel, "Suporte" brings it back, focus returns to it | DONE 2026-09-14 |
| BL-016 | Decision (audit FND-0026) | Role model: may a non-owner agent set status, resolve, close or open a consultation on a colleague's case? Attribution is preserved today | RULE-SUP-02/-09 design question | Decided in PH-7.2: no — owner or supervisor/admin (DEC-0029), enforced by the API and mirrored by the workspace | DONE 2026-09-14 |
| BL-017 | Tech debt (audit FND-0027) | Dev-only advisories: `drizzle-kit` → `@esbuild-kit/*` / esbuild, `@nestjs/mau` → undici/tmp/inquirer | Not shipped; noise in `npm audit` | Done in PH-12.1 (DEC-0043 d): `@nestjs/mau` removed — its undici advisories (2 high, 5 moderate, 2 low) are gone; `npm audit --omit=dev` stays at 0. Remaining: 4 moderate dev-only advisories in the loader of drizzle-kit (`@esbuild-kit` → esbuild dev server), never shipped; the only fix is a breaking downgrade of drizzle-kit — accepted, revisit at the next drizzle-kit major | DONE (dev-only remainder accepted) 2026-09-14 |
| BL-018 | Tech debt (audit FND-0027) | Shared response schemas (`caseSummarySchema`, `staffCaseDetailSchema`, …) are exported but unused: use them to validate API responses in e2e, or prune | Contract drift would go unnoticed | Done in PH-9.1: the e2e suite parses case, message, consultation and queue responses with the shared schemas in strict mode, with a negative case (a staff summary is refused as a customer summary) | DONE 2026-09-14 |
| BL-020 | Process (FND-0028) | Commits `3217c53` and `6d11ee8` were created after `npm run verify` failed at `check-context` (first: chain used `;`; second: a heredoc ended the `&&` chain); CI red for two commits, each corrected by the next | Gate discipline (§7.2, §9.2) | Agent: commits are produced only by a gate-then-commit script (`verify` exit 0 → commit → push), never by an inline chain; a git pre-commit hook running `check:context` | DONE 2026-09-14 (Cycle Audit 2 FND-0040): `scripts/gate-commit.sh` (`npm run gate`) and `scripts/git-hooks/pre-commit` are in the repository; enable the hook with `git config core.hooksPath scripts/git-hooks` |
| BL-019 | Verification (audit FND-0009) | Run the api unit and e2e suites against a server PostgreSQL (service container) to confirm the row-lock semantics of `mutate()` under a real pool | Concurrency fixes are verified on PGlite's serialized connection only | Done in PH-8.2 (DEC-0032): `SUPPORT_DATABASE_URL` driver, `test:pg`, Docker compose for local runs, CI job `verify-postgres` (the required verdict) | DONE 2026-09-14 (CI verdict recorded in `evidence/PH-8.2-verification.md`) |
| BL-021 | Product (audit FND-0044) | "Waiting since" for `waiting_internal` cases (status-change time or the open consultation's `createdAt`) in `CaseSummary` and the queue label; long-pending consultations in the supervision overdue list (or a separate list) | §5.2 "cases requiring follow-up", §5.4, §14.9: a case waiting for a team never becomes overdue and shows no age | Done in PH-8.1: `waiting_internal_since` (migration `0017`), queue label "Aguardando a equipe há …", overview `waitingInternal`, overdue list includes waiting-for-team cases | DONE 2026-09-14 |
| BL-022 | Tech debt (audit FND-0046, FND-0051) | Expose `CasesService.remind(caseId, now)` so the reminder job does not own a second lock convention; one `awaitingReplySince` helper (shared) instead of three copies; tests for the job `tick()` paths (single-flight, error logging, timer clearing) | DEC-0017 conformance and maintainability; no observable defect | Done in PH-9.1: `CasesService.remind` decides under the case lock, `apps/api/src/cases/case-rules.ts` owns `awaitingReplySince` and its SQL twin (agreement tested), `jobs.spec.ts` covers the three jobs' `tick()` paths | DONE 2026-09-14 |
| BL-023 | Process (audit FND-0053) | Isolate the UI smoke's blocks: own customers or a database reset per block and references taken from API responses instead of counted; keep the harness lessons out of per-subphase patches | `scripts/ui-smoke.mjs` changed in 20 commits; ordering-dependent assertions cost a rerun per subphase | Done in PH-9.4 (DEC-0038 d): the smoke reads every case reference from the product (`shownReference`); none is hard-coded. Blocks still share one database and run in order — with references read, a per-block reset is not needed | DONE 2026-09-14 |
| BL-024 | UX/a11y (audit FND-0041, FND-0056) | Focus management after "Voltar" and the bell list; queue tabs as real tabs (panels, arrow keys) or plain buttons; duration units and the outside-hours copy through the dictionary; settings number fields keep raw text until submit; removal confirmation copy | §10.1 honesty and accessibility basics; no data defect | Done: focus after "Voltar", dictionary units and recovery form copy (PH-8.1, cycle 3 audit); queue tabs, number fields and system-message copy through the dictionary in PH-9.2 (DEC-0036 a/c/d) | DONE 2026-09-14 |
| BL-026 | Security/ops (audit FND-0070) | `Retry-After` on every 429 and a proxy-aware per-client limit for the recovery route (the instance-wide ceiling of 120 per 10 minutes is only a flood net) | One source can still keep the only route for people who cannot sign in busy (§4.5) | Done in PH-9.4 (DEC-0038 a/b): `Retry-After` on every 429 (`RetryAfterFilter`); per-client limit of 10 per 10 minutes behind a reverse proxy that appends `X-Forwarded-For` (`SUPPORT_TRUST_PROXY`, unset by default: the web's proxy does not report the browser's address). Condition for any deployment beyond loopback: set it with the TLS proxy (`runbooks/DEPLOYMENT.md`) | DONE 2026-09-14 |
| BL-027 | Privacy (audit FND-0074) | Detect likely passwords/codes pasted into recovery descriptions and mask them for staff, with the original kept only for the verification process | Staff can read a secret the person should never have typed (§4.5, §10.2); the staff page now warns | Done in PH-12.1 (DEC-0043 a): `maskLikelySecrets` in the staff projection, the original kept in the row; the staff page notes the masking; extended by the closing audit (FND-0108: es/pt keywords, spaced digits, all-caps codes, seed phrases as word lists). Heuristic — the real verification hand-off (BL-001) keeps the original | DONE 2026-09-14 |
| BL-028 | Reliability (audit FND-0076) | E-mail attempts counter and a dead-letter state for sends that keep failing; state the at-most-once loss window in the provider integration | Today a failing send is retried every tick forever; a crash between claim and send loses one e-mail (the in-product notification survives) | Done in PH-12.1 (DEC-0043 b): `email_attempts` / `email_failed_at` (migration `0023`), `SUPPORT_EMAIL_MAX_ATTEMPTS` (5), dead letters logged and never retried. The at-most-once window (a crash between claim and send) stays as documented in FEAT-NOTIFY; a real provider (Owner) may add its own receipts | DONE 2026-09-14 |
| BL-029 | Product (audit FND-0078) | Count cases with an open consultation as waiting for a team in supervision even when staff moved them to `in_progress` | Such cases leave the waiting-for-team count and the overdue list while a team still owes an answer (§5.4) | Done in PH-9.1 (DEC-0035 a/b): waiting for a team = `waiting_internal` or an open consultation, whatever the status; the overdue list holds each case once | DONE 2026-09-14 |
| BL-030 | Process (audit FND-0080) | `check-context` blind spots: paths inside commands and code spans in `.sh`/`.sql`/Dockerfiles/`.env.example`, freshness metadata vs Git, tag names and CI ids | Stale statements the checker cannot see keep recurring (FND-0022, FND-0037, FND-0063) | Done in PH-9.4 and PH-12.1 (DEC-0038 e, DEC-0043 c): command spans, cited commits and tags, out-of-band records, and repository paths in `.sh`/Dockerfiles/compose/`.env.example`/CI. Not checked by design: CI run ids (network) and prose freshness | DONE 2026-09-14 (limits recorded) |
| BL-031 | Product (Owner request, 2026-09-14) | AI assistant as the first line of support (the Owner named ChatGPT): answers from approved content and authorized account data, transfers to a person when the customer asks or it cannot help, never moves money or changes results (`PROJECT_CONTEXT.md` §12, RULE-SUP-10, DEC-0039 b) | The Owner wants it in front of the 24/7 human team | Owner: provider account and cost (paid service), and the build itself — directly with GPT from `phases/PH-11.md` (the brief; Owner, 2026-09-14) | PLANNED (PH-11, Owner's build) |
| BL-032 | Decision / dependency (PH-13.1) | Backend answers the live Orbit connection needs (`integration/ORBIT-INTEGRATION.md` §7): token verification material and the person-id claim; a service credential for `/admin/rastreio` and `/admin/team-members`; tenant header; enumerations (`status`, `kyc_status`, methods); protocol numbers; logout/refresh semantics; notification backend and consent; partner-chat reuse; staff `/me`; edge CSP/CORS and the anti-inspection exemption; data residency | Without them the adapters cannot be exercised against a real environment (PH-13.3). Narrowed by DEC-0046 (2026-09-14): token verification (by the broker), the credential (a read-only team member), the roles and the embedding are decided; what remains is the account itself, dev access, the enumerations/protocol numbers, the edge policy (CSP `frame-ancestors`, anti-inspection exemption) and the staff session | Owner: create the service account (`auditor`, no 2FA) and grant dev access; Agent: PH-13.3 | OPEN (narrowed) — blocks PH-13.3 |
