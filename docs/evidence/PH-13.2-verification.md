# PH-13.2 — Verification evidence
Type: VERIFICATION EVIDENCE
Work item: PH-13.2 — The panel inside the broker (DEC-0046)
Recorded on: 2026-09-14
Revision: base `cbcd3b4` plus the PH-13.2 working tree, committed as the next commit on `main` (this record travels with it — §3.4).
Environment: Windows 11 native, Git Bash, Node v24.19.0, npm 11.17.0. No network to the broker: fixtures and a fake `fetch`; jsdom for the web.

## Claims
| # | Claim | Category | Method / command | Result |
|---|---|---|---|---|
| 1 | Configuration: no local material needed for identity (both refused); the service credential is a static token or e-mail + password (each missing one named) | EXECUTED | `npm test -w api` (`optaqode.spec.ts`) | passed |
| 2 | Identity verified by the broker: `/profiles/me` names the customer; cached by hash and presented again after `ORBIT_TOKEN_CACHE_MS`; 401 → nobody; malformed id → nobody; staff by an admin read plus the directory; an outsider and a customer token claiming staff → nobody; a timeout → nobody; the service password never appears in any request the identity makes | EXECUTED | same | passed |
| 3 | Identity verified locally when a secret is set: claim id, no call to the broker, the same refusals | EXECUTED | same | passed |
| 4 | Service session: one login, reuse, refresh inside the 60 s margin, login again on a refused refresh, no bearer on auth calls, 2FA challenge → `OptaqodeServiceAccountError`; `createTokenSource` picks the static token when present | EXECUTED | same | passed |
| 5 | Records adapter retries once with a fresh service token on 401 | EXECUTED | same | passed |
| 6 | Role map: `super_admin` → admin, `admin` → supervisor, `regional_admin`/`finance_manager`/`product_manager`/`auditor` → agent | EXECUTED | same | 15 tests passed (the file) |
| 7 | Web `/embed`: nothing identity-bound before a session; a session from another origin ignored; after an allowed session `/identity/me` is called with the bearer only (no simulated header), the panel shows no "Conta simulada" strip and posts `signed-in`; sign-out returns to waiting; a refused token posts `token-required` and shows nobody's data; no allowed origins → refused; protocol helpers parse origins strictly | EXECUTED | `npm test -w web` (`EmbeddedSupport.test.tsx`) | passed |
| 8 | Every earlier web and API contract unchanged | EXECUTED | `npm test -w web`, `npm test -w api`, `npm run test:e2e -w api`, `npm test -w @orbit-support/shared` | 117; 114 passed + 1 skipped; 39; 27 |
| 9 | Static layers and the checker | EXECUTED | `npm run verify` | exit 0 |
| 10 | Full gate on the candidate | EXECUTED | `bash scripts/gate-commit.sh` | the `Gate-Verified` trailer of the commit |
| 11 | CI on the pushed commit | NOT VERIFIED at recording time | `.github/workflows/ci.yml` | recorded in the next state update (fails on the missing remote tag until the Owner pushes `v0.2.0-demo`) |

## Limitations
- The broker's backend was not reached: `/profiles/me` as a verification oracle, `/admin/auth/login` for the service account and `/admin/rastreio` for records are the frontend's contracts, exercised with fakes; PH-13.3 runs them for real.
- The host component is a proposal for the broker's repository, not compiled or tested here; the iframe hand-off is tested from the panel's side only (jsdom `MessageEvent`).
- The staff workspace keeps the simulated picker; a staff session through the broker's back-office token is designed (`x-orbit-actor: staff`) but has no web surface yet.
