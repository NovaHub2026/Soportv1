# Release record — internal demo v0.1.1-demo (supersedes v0.1.0-demo)
Type: RELEASE RECORD
Recorded on: 2026-09-14
Supersedes: `RELEASE-2026-09-14.md` (tag `v0.1.0-demo`, never moved — §9.3)
Candidate: the cycle 3 out-of-band audit remediation commit (child of `dcc76e4`); tag `v0.1.1-demo` is created on it only after its CI is green on both jobs (FND-0063), in the closure commit.
Environment: this Windows 11 host only — loopback (127.0.0.1), API :3001, web :3000, embedded PostgreSQL under `apps/api/.data/demo` (gitignored); `node scripts/demo-local.mjs`.

## Why a corrected release
The out-of-band audit (`../audits/CYCLE-3-OOB.md`) found MATERIAL defects visible in the demo: the first render of a signed-out device showed the default customer's signed-in screen and sent its requests (FND-0060), an agent was offered edit controls the API refuses and could leave an orphan incident (FND-0061), and the previous record left its own CI verdict unrecorded (FND-0063). §9.3: a wrong release is superseded by a new record; the old tag stays.

## Authorization (GOVERNANCE.md §1.1)
- Same scope as `RELEASE-2026-09-14.md`: the Owner's «Ejecuta todo en orden» (2026-09-14) read as an internal, access-restricted demo on this machine. A corrective re-release inside that scope follows §9.3.
- **Pending the Owner's confirmation:** release gate #5 (operating policies, BL-002) is not met; the Agent treated it as acceptable for an internal demo whose screens label every working default. The Owner should confirm or withdraw that reading (FND-0063, COLD-12).

## Release gate (docs/runbooks/RELEASE.md)
| # | Check | Result |
|---|---|---|
| 1 | No open audit, no open MATERIAL finding | the out-of-band audit's MATERIAL findings are fixed in this candidate; the audit closes in the same commit that tags the release, after CI |
| 2 | CI green on the candidate, both jobs | required before tagging — recorded in the closure commit |
| 3 | `verify:full` and the browser smoke | met — see `CYCLE-3-verification.md` (suites 23/66/34/78, builds exit 0, smoke 54/54) |
| 4 | BL-012, BL-019 closed; BL-001 | met for a demo — BL-001: Orbit does not exist, so identity and records are simulated; demo only |
| 5 | Operating policies (BL-002) | **not met — pending the Owner's confirmation** (above) |
| 6 | Configuration | met — `NODE_ENV=production`, `SUPPORT_BIND=127.0.0.1`, demo switch, PGlite |
| 7 | Backup/restore and rollback; container rehearsal | partly — demo database directory copy with the API stopped; rollback = the previous tag; the container rehearsal is **not executed** (Docker engine blocked on this host) |
| 8 | Release notes | below |
| 9 | No carried backlog item past its revisit event (FND-0065) | met — BL-022/BL-023/BL-024 now carry "next release candidate beyond the demo" as their event |

## Release notes (since v0.1.0-demo)
Security and privacy: identity-neutral first render (no customer or agent shown or queried before the browser's identity is known), sign-out clears drafts in every tab, idle timer shared across tabs and counting scrolls inside the panel; staff display names from the directory; recovery contacts normalized for the per-contact limit and refusing control/bidi characters; honest "muitos pedidos no momento" when the route itself is busy; safe deployment defaults (demo switch off, web on 127.0.0.1, API not published, image builds with the API address). Workspace: context column and incident controls follow the role model; queue results never cross views or agents; exact "more" detection; saved-reply removal with cancel; recovery page never shows another filter's rows. Reliability: migration lock and pool error handling on PostgreSQL; upload quota counts only accepted files; limiter and stream maps shrink. Tooling: tests refuse any database not named `*_test`; the gate stages only named new files. Migration `0018`.

## Deployment and post-release checks
`node scripts/demo-local.mjs --check` on the remediated build with a fresh demo database, exit 0:

| Check | Result |
|---|---|
| `/api/health` through the web rewrite | `ok`, `database: pglite (embedded PostgreSQL)`, `identity: simulated`, `orbitRecords: simulated` |
| Web home | 200 |
| Security headers on the API | `x-content-type-options: nosniff`, no `x-powered-by` |
| §14 item 2 round-trip | `SUP-000001` created; reply by "Ana Ribeiro" (directory name) visible to the customer |
| Notification of the reply | `staff_reply` present |
| RULE-SUP-01: another customer | 404 |
| Server render identity-neutral (FND-0060) | no customer name, no "Sair" in the HTML |

## Outcome
Prepared as the internal loopback demo `v0.1.1-demo`; the tag is placed by the closure commit once CI is green. Not a production release (BL-001, BL-002, §1.1).
