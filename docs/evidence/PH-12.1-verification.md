# PH-12.1 — Verification evidence
Type: VERIFICATION EVIDENCE
Work item: PH-12.1 — Closable debt (BL-017, BL-027, BL-028, BL-030; DEC-0043)
Recorded on: 2026-09-14
Revision: base `9cba953` plus the PH-12.1 working tree, committed as the next commit on `main` (this record travels with it — §3.4).
Environment: Windows 11 native, Git Bash, Node v24.19.0, npm 11.17.0. Sequential runs; in-memory PGlite for the suites.

## Claims
| # | Claim | Category | Method / command | Result |
|---|---|---|---|---|
| 1 | `maskLikelySecrets`: a password after "senha é", a code after "código", "password: …", a bare 6-digit number and a recovery phrase are masked; "A senha nunca chega.", "O código não chega no e-mail.", a phone number and a dated reference are untouched | EXECUTED | `npm test -w @orbit-support/shared` (`access.test.ts`) | 27 tests passed |
| 2 | Recovery service: staff read "Minha senha é [oculto] e o código [oculto] expirou." with `descriptionMasked: true` while the stored row keeps "Abc12345"; a description about a code is untouched with `descriptionMasked: false` | EXECUTED | `npm test -w api` (`access-recovery.service.spec.ts`) | 98 passed, 1 skipped (PostgreSQL-only) |
| 3 | Dead letters: with the notifier throwing, five due runs count attempts 1..5, keep `emailedAt` null, park the row at the fifth (`emailFailedAt`); a sixth run and a run after the provider recovers call nothing; the in-product notification is still listed; nothing in the outbox | EXECUTED | `npm test -w api` (`cases.service.spec.ts`, PH-12.1) | passed (within claim 2) |
| 4 | HTTP: a request whose description carries a code reaches staff as "O código [oculto] que recebi não funciona." with `descriptionMasked: true`; the earlier request is `descriptionMasked: false`; every earlier contract unchanged | EXECUTED | `npm run test:e2e -w api` | 39 tests passed |
| 5 | Web: the staff page notes a masked description; every earlier test unchanged after the recovery fixture gained the new fields | EXECUTED | `npm test -w web` | 105 tests passed |
| 6 | Migration `0023` adds `email_attempts` (default 0) and `email_failed_at`; schema and migrations agree | EXECUTED | `npm run db:generate -w api` | one file, two `ADD COLUMN` |
| 7 | Checker, positive: the tree passes with the operational-file rule (Dockerfiles, compose, `.env.example`, CI, shell scripts) | EXECUTED | `npm run check:context` | OK at commit time |
| 8 | Checker, negative (§7.3): in a disposable worktree of `9cba953` with the new checker, a `COPY apps/api/nope-probe …` line in `docker/api.Dockerfile` and a `docs/runbooks/NOPE.md` in `.env.example` each fail with "repository path not found"; the worktree was removed | EXECUTED | `git worktree add` + `node scripts/check-context.mjs` | the intended failures |
| 9 | BL-017: after removing `@nestjs/mau`, `npm audit` no longer lists undici (before: 9 advisories — 2 high, 5 moderate, 2 low); `npm audit --omit=dev` reports 0; 4 moderate dev-only advisories remain in drizzle-kit's loader (`@esbuild-kit` → esbuild ≤ 0.24.2, a development-server advisory), whose only offered fix is a breaking downgrade of drizzle-kit (0.18.1) | EXECUTED | `npm install`, `npm audit`, `npm audit --omit=dev`, `npm audit fix --dry-run` | as stated — remainder accepted (DEC-0043 d) |
| 10 | Static layers | EXECUTED | `npm run typecheck`, `npm run lint` (both apps) | exit 0 |
| 11 | Full gate on the candidate | EXECUTED | `bash scripts/gate-commit.sh` | the `Gate-Verified` trailer of the commit |
| 12 | CI on the pushed commit | NOT VERIFIED at recording time | `.github/workflows/ci.yml` | recorded with the closing audit |

## Limitations
- Masking is heuristic: a secret written as an ordinary word passes; a bare 6–8 digit reference is hidden from staff (the original stays in the row).
- Dead letters are visible in the API log only; no alert channel exists (the notification provider is the Owner's decision).
- No browser run: the only screen change is a note under a masked description, covered by the unit test of the panel's copy path and the e2e contract.
