# Release record — internal demo v0.1.0-demo
Type: RELEASE RECORD
**Superseded** by `RELEASE-2026-09-14b.md` (tag `v0.1.1-demo`) after the cycle 3 out-of-band audit (FND-0060, FND-0061, FND-0063). This record and the tag `v0.1.0-demo` are kept unchanged otherwise (§9.3). CI of the release commit `dcc76e4`: run 34837013994 **success**, both jobs (recorded by the audit — it was missing, FND-0063).
Recorded on: 2026-09-14
Candidate: ``b04f0ac` (code and gate) plus the release commit that carries this record and `scripts/demo-local.mjs`` on `main` (tag `v0.1.0-demo`)
Environment: this Windows 11 host only — loopback (127.0.0.1), API :3001, web :3000, embedded PostgreSQL persisted under `apps/api/.data/demo` (gitignored); started with `node scripts/demo-local.mjs`.

## Authorization (GOVERNANCE.md §1.1)
- Who: the Owner, in the session of 2026-09-14 — «Ejecuta todo en orden», in reply to the report that offered (1) the container rehearsal, (2) an access-restricted demo release or new phases, (3) an out-of-band Cycle Audit 3.
- Scope read by the Agent: an **internal, access-restricted demo** on this machine. Not authorized and not done: any public or hosted deployment, a domain, TLS, hosting or any paid service, a mail provider, any action with real customer data.
- Conditions: the demo runs with the simulated identity, records and e-mail (DEC-0003, DEC-0008 demo switch), labeled as simulation on every screen; it is reachable only from this machine.
- "New phases" (real Orbit adapters, a mail provider) were not opened: Orbit does not exist yet (DEC-0003, BL-001) and a mail provider is a paid-service decision (§1.1). They stay Owner decisions.

## Release gate (docs/runbooks/RELEASE.md)
| # | Check | Result |
|---|---|---|
| 1 | No open Cycle Audit, no open MATERIAL finding | met — Cycle Audit 2 CLOSED; cycle 3 at 2/3 (no audit due); an out-of-band Cycle Audit 3 follows this release as the Owner's step 3 and may supersede it (§9.3) |
| 2 | CI green on the candidate, both jobs | met — `b04f0ac`: run 34827966599 **success** (jobs `verify` + build and `api suites on PostgreSQL 16`) |
| 3 | `npm run verify:full` and the browser smoke on the candidate | met — `npm run verify:full` exit 0 on `b04f0ac` (check-context OK; shared 21/21, api 65/65, web 73/73, api e2e 34/34; builds exit 0); browser smoke 54/54, screenshots `screenshots/release-demo/01`–`29` |
| 4 | BL-012 and BL-019 closed; BL-001 open | met for a demo — BL-012 and BL-019 DONE; BL-001 open, hence demo-only |
| 5 | Operating policies decided (BL-002) | **not met** — accepted for an internal demo: the product shows its working defaults labeled as such (`docs/runbooks/OPERATIONS.md`) |
| 6 | Configuration reviewed | met — `NODE_ENV=production`, `SUPPORT_BIND=127.0.0.1`, `SUPPORT_ALLOW_SIMULATED_IDENTITY=true` (demo switch), PGlite (single-host demo per DEPLOYMENT.md) |
| 7 | Backup and restore rehearsed; rollback known | partly — the demo database is a directory (`apps/api/.data/demo`): backup = copy it with the API stopped; rollback = check out the previous tag and rebuild. The PostgreSQL/`pg_dump` path and the container rehearsal are **not executed**: Docker Desktop's engine on this host fails to start (see "Blocked") |
| 8 | Release notes | below |

## Release notes (since the first commit; phases PH-1..PH-8)
Customer panel inside the simulated Orbit host (cases with references, live conversation, attachments, reopening and linked follow-ups, record cards and contextual entry, availability from the configured schedule, in-product notifications, simulated e-mail outbox with opt-out, outside-hours notice, reminders, access-recovery route, sign-out and idle sign-out); staff workspace (seven queues, search and filters, saved replies, internal notes, consultations, transfers, shared incidents, Orbit context masked and labeled, supervision with overview, overdue list and metrics, settings, recovery requests, role model); hardening (headers, JSON 404, stream and upload limits, loopback bind); PostgreSQL verification path. Migrations `0000`–`0017` apply on start. Decisions DEC-0001..DEC-0033; audits CYCLE-1, CYCLE-2 closed.

## Deployment and post-release checks
`node scripts/demo-local.mjs --check` on this host after the smoke, exit 0 — API and web started with `NODE_ENV=production`, the demo switch and `SUPPORT_BIND=127.0.0.1`; report:

| Check | Result |
|---|---|
| `/api/health` through the web rewrite | `ok`, `database: pglite (embedded PostgreSQL)`, `identity: simulated`, `orbitRecords: simulated` |
| Web home | 200 |
| Security headers on the API | `x-content-type-options: nosniff`, no `x-powered-by` |
| §14 item 2: a question becomes a case, staff reply, customer sees it | `SUP-000001` created, reply visible to the customer |
| Notification of the reply | `staff_reply` notification present |
| RULE-SUP-01: another customer asks for that case | 404 |

The demo is stopped after the check; start it with `node scripts/demo-local.mjs` (Ctrl+C stops it). Its data persists in `apps/api/.data/demo`.

## Blocked
Container rehearsal (DEPLOYMENT.md "First start"): Docker Desktop 4.87 fails at start — `initializing Ingest server: … remove C:/Users/alejo/AppData/Local/Docker/run/sailor-ingest.sock: El sistema no tiene acceso al archivo`. Four stale socket entries in `%LOCALAPPDATA%\Docker\run` (dated 21/08/2026) cannot be read or removed without elevation (`del` and `icacls` refused). Owner action: in Docker Desktop's error dialog choose "Reset to factory defaults", or remove those four entries from an administrator terminal, or reboot; then run `docker compose -f docker/compose.yml up -d --build` and record it in `docs/evidence/PH-8.3-verification.md`.

## Outcome
Released as an **internal, loopback-only demo** tagged `v0.1.0-demo` on the release commit. Not a production release: identity, records and e-mail are labeled simulations (BL-001), the operating policies are pending (BL-002), and the container rehearsal is blocked on this host. The out-of-band Cycle Audit 3 (the Owner's step 3) audits this tag; a MATERIAL finding would supersede this record with a new one (§9.3), never move the tag.
