# PH-10.1 — Verification evidence
Type: VERIFICATION EVIDENCE
Work item: PH-10.1 — Schedule and time zone (DEC-0039 a, DEC-0040)
Recorded on: 2026-09-14
Revision: base `88993a5` plus the PH-10.1 working tree, committed as the next commit on `main` (this record travels with it — §3.4).
Environment: Windows 11 native, Git Bash, Node v24.19.0, npm 11.17.0, Playwright Chromium (headless); the machine's zone is America/Sao_Paulo, so the browser runs show the customer's zone equal to the operation's. Sequential runs; in-memory PGlite for the suites, a scratch PGlite directory for the smoke.

## Claims
| # | Claim | Category | Method / command | Result |
|---|---|---|---|---|
| 1 | The decided default schedule is every day 00:00–24:00; with it the availability is open, `alwaysOpen`, with no next opening and today's window as instants (Saturday 23:30 in São Paulo → 03:00 UTC to 03:00 UTC next day) | EXECUTED | `npm test -w @orbit-support/shared` (`settings.test.ts`, PH-10.1) | 24 tests passed |
| 2 | Instants follow the zone: Wednesday 09:00–18:00 São Paulo → 12:00–21:00 UTC and the next opening Thursday 09:00 → 12:00 UTC; Madrid 09:00 in September → 07:00 UTC; "24:00" São Paulo → 03:00 UTC next day; "24:00" refused as an opening time, "24:01" refused | EXECUTED | same | passed (within claim 1) |
| 3 | The earlier opening/closing rules hold on a weekday schedule (open inside the window, next opening, FND-0043 next-week case, unknown zone fails closed) | EXECUTED | same | passed (within claim 1) |
| 4 | API: the availability endpoint reports `alwaysOpen: true`, `nextOpeningAt: null` and a `todayWindow` with the always-open schedule; the outside-hours notice still fires when every day is closed; every earlier contract unchanged with the setups' 00:00–24:00 schedule | EXECUTED | `npm run test:e2e -w api`, `npm test -w api` | 37 passed; 92 passed, 1 skipped (PostgreSQL-only) |
| 5 | Web home: a narrowed schedule shows today's window and the next opening formatted by the browser (expected values computed with the same clock in the test) and "Horários no seu fuso (<zone>)"; a 24/7 schedule says "Atendimento 24 horas, todos os dias." with no window or opening; an all-day today inside a narrowed week says "Hoje: atendimento 24 horas."; an outside-hours notice with `nextOpeningAt` is worded in the customer's zone; the settings form names the Owner's decision instead of a working default | EXECUTED | `npm test -w web` (`SupportHome.test.tsx`, `ConversationDebt.test.tsx`, `StaffWorkspace.test.tsx`) | 101 tests passed |
| 6 | Browser: the customer home states the decided 24/7 service and the customer's zone; after the supervisor unchecked "Dia inteiro em Sábado" the home stopped saying 24 hours ("Atendimento aberto agora. Hoje: atendimento 24 horas." on a weekday); with every day closed the outside-hours notice appeared as "Aviso"; every PH-1..PH-9 journey unchanged | OBSERVED | `npm run build`; `SUPPORT_DB_DIR=<scratch> SUPPORT_UPLOADS_DIR=<scratch> node scripts/ui-smoke.mjs <scratch>` | exit 0, 56 observations (screenshots kept in scratch — DEC-0034 c) |
| 7 | Course correction: the first smoke run (same code but the home) showed "Hoje: 00:00–00:00" for an all-day weekday in a narrowed week; the copy was changed to "Hoje: atendimento 24 horas." with a unit test, and the smoke rerun on the final code (claim 6) | OBSERVED | first smoke run, scratch | as stated |
| 8 | Static layers and production builds | EXECUTED | `npm run typecheck`, `npm run lint`, `npm run build` | exit 0 |
| 9 | Full gate on the candidate | EXECUTED | `bash scripts/gate-commit.sh` | the `Gate-Verified` trailer of the commit |
| 10 | CI on the pushed commit | NOT VERIFIED at recording time | `.github/workflows/ci.yml` | recorded in the next state update |

## Limitations
- The customer's zone is the browser's until Orbit's session carries one (BL-001); the smoke machine's zone equals the operation's, so a different-zone rendering is established by the unit tests (claim 5), not observed.
- Staff timestamps stay in the browser's zone (out of scope).
