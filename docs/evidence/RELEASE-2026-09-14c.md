# Release record — internal demo v0.2.0-demo (everything since v0.1.1-demo)
Type: RELEASE RECORD
Recorded on: 2026-09-14
Previous: `RELEASE-2026-09-14b.md` (tag `v0.1.1-demo` on `e75b153`, kept — §9.3)
Candidate: `dfd7a41` (the closing audit remediation commit, child of `33e8794`); tag `v0.2.0-demo` created on it after CI run 34875569705 was green on both jobs (DEC-0034 d, FND-0063); the push of the tag to `origin` was refused by the Agent's runtime as an outward-facing action and is the Owner's one command (`git push origin v0.2.0-demo`), recorded in `../../SESSION_HANDOFF.md`.
Environment: this Windows 11 host only — loopback (127.0.0.1), API :3001, web :3000, embedded PostgreSQL under `apps/api/.data/demo` (gitignored); `node scripts/demo-local.mjs`.

## Why this release
PH-12 closes the project except PH-11 (the Owner's GPT build). The internal demo must carry everything delivered since `v0.1.1-demo`: PH-9 (release-candidate debt), PH-10 (the Owner's operating policies in the product), PH-12.1 (closable debt) and the closing audit's remediation. Same scope as the previous demos: an internal, access-restricted demo on this machine under the Owner's «Ejecuta todo en orden» and «finalizar todas las cosas pendientes» (2026-09-14); no production release is authorized (§1.1).

## Authorization (GOVERNANCE.md §1.1)
- Internal loopback demo within the scope the Owner gave; not a production release. Gate #5's two open policies (retention, e-mail provider) are pending on the Owner's side by decision (DEC-0039), which an internal demo may carry — the Owner confirms or withdraws that reading by keeping or superseding this record (FND-0063).

## Release gate (docs/runbooks/RELEASE.md)
| # | Check | Result |
|---|---|---|
| 1 | No open audit, no open MATERIAL finding; ledger current | met — `../audits/CLOSING.md` closed on this candidate; cycles 1–3 closed; cycle 4 at 1/3 → 2/3 with PH-12's approval (`PH-12-phase-approval.md`) |
| 2 | CI green on the candidate, both jobs | met — run 34875569705 **success** (`verify` + build, `api suites on PostgreSQL 16`) |
| 3 | `verify:full` and the browser smoke on the candidate | met — `npm run verify:full` exit 0 (shared 27, api 99 + 1 skipped, web 113, e2e 39; both production builds); smoke exit 0, 59 observations, screenshots `screenshots/release-v0.2.0-demo/` (34 files, 4.5 MB — the release set, DEC-0034 c) |
| 4 | BL-012, BL-019 closed; BL-001 | met for a demo — BL-001: Orbit does not exist, so identity and records are simulated; demo only |
| 5 | Operating policies (BL-002) | met for a demo — decided by the Owner (DEC-0039) and in the product since PH-10; retention and the e-mail provider stay the Owner's (nothing is deleted; e-mail is a simulated outbox) |
| 6 | Configuration | met — `NODE_ENV=production`, `SUPPORT_BIND=127.0.0.1`, demo switch, PGlite; `SUPPORT_TRUST_PROXY` unset (loopback, no proxy) |
| 7 | Backup/restore and rollback; container rehearsal | partly — demo database directory copy with the API stopped; rollback = `v0.1.1-demo`; the container rehearsal was executed once on `a863593` (PH-8.3 addendum) and not repeated for this demo |
| 8 | Release notes | below |
| 9 | No carried backlog item past its revisit event (FND-0065) | met — BL-005/BL-006 wait for their toolchain triggers; BL-022/023/024 were paid in PH-9; nothing else is carried |

## Release notes (since v0.1.1-demo)
- PH-9 — release-candidate debt: waiting-for-a-team as a queue view and overdue rule, workspace rules (`runAction`, tabs, number fields), the customer panel's first-message attachment and desktop close, recovery `Retry-After`, per-client limit behind an appending proxy (`SUPPORT_TRUST_PROXY`), the checker's command-span, commit and tag rules; migrations `0019`–`0020`.
- PH-10 — the Owner's operating policies (DEC-0039): 24/7 as the default schedule with "24:00" closings and times shown in the customer's own zone (DEC-0040); formal complaints as a customer topic, worked by supervisors only, with an internal five-business-day deadline and a supervision list (DEC-0041; migration `0021`); an administrator's recorded export of one customer's support data; recovery requests forwarded to the Verification team by name (DEC-0042; migration `0022`).
- PH-12.1 — likely secrets masked in recovery descriptions for staff; e-mail attempts and dead letters (`SUPPORT_EMAIL_MAX_ATTEMPTS`; migration `0023`); the checker reads repository paths in operational files; the unused Nest deploy CLI removed with its advisories (DEC-0043).
- Closing audit (`../audits/CLOSING.md`, DEC-0044): exports carry only the customer-visible history without staff ids; the complaint rule covers every route and a reclassified case leaves an agent owner; the customer note promises no response time; masking widened; dead letters race-free; the checker exact-case; availability wording by the customer's own day; export and complaint copy honest.
- Migrations since the previous demo: `0019`, `0020`, `0021`, `0022`, `0023` (`apps/api/drizzle/`).

## Deployment and post-release checks
`node scripts/demo-local.mjs --check` on the tagged build with a fresh demo database, exit 0:

| Check | Result |
|---|---|
| `/api/health` through the web rewrite | `ok`, `database: pglite (embedded PostgreSQL)`, `identity: simulated`, `orbitRecords: simulated` |
| Web home | 200 |
| Security headers on the API | `x-content-type-options: nosniff`, no `x-powered-by` |
| §14 item 2 round-trip | `SUP-000001` created; reply by "Ana Ribeiro" (directory name) visible to the customer |
| Notification of the reply | present |
| RULE-SUP-01: another customer | refused |
| Server render identity-neutral (FND-0060) | no customer name, no "Sair" in the HTML |

## Outcome
Released as the internal loopback demo `v0.2.0-demo` (tag on `dfd7a41`, after green CI). Not a production release (BL-001, the two open policies, §1.1).
