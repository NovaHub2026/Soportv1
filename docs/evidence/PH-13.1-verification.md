# PH-13.1 — Verification evidence
Type: VERIFICATION EVIDENCE
Work item: PH-13.1 — Integration map and API adapters (DEC-0045, BL-001, BL-032)
Recorded on: 2026-09-14
Revision: base `aa82f75` plus the PH-13.1 working tree, committed as the next commit on `main` (this record travels with it — §3.4).
Environment: Windows 11 native, Git Bash, Node v24.19.0, npm 11.17.0. No network to the broker: every adapter test uses fixtures transcribed from the broker frontend's types and a fake `fetch`.

## Claims
| # | Claim | Category | Method / command | Result |
|---|---|---|---|---|
| 1 | The broker's frontend was analysed read-only: `git status` of `optaqode-frontend2.0` unchanged; no build, install or server run there | INSPECTED | the reviewer's report; the map's sources are paths inside that repository | as stated |
| 2 | Configuration: the base URL is required and must be https (localhost excepted); the identity adapter needs exactly one of secret/JWKS; the records and directory adapters name `ORBIT_SERVICE_TOKEN`; a non-positive timeout is refused | EXECUTED | `npm test -w api` (`optaqode.spec.ts`, "configuration") | passed |
| 3 | Selection: simulated by default for all three adapters; `optaqode` refused without its material; unknown names refused; the simulated provider still refuses `NODE_ENV=production` without the opt-in; the directory follows the records adapter unless set | EXECUTED | same | passed |
| 4 | Client: bearer and locale headers sent; `error_code` + joined message list parsed; a slow broker becomes `OptaqodeTimeoutError` | EXECUTED | same | passed |
| 5 | Token verification: HS256 valid → payload; bad signature, expired, wrong issuer, wrong audience, `alg: none`, malformed → null; ES256 through a JWKS document with one refresh on an unknown `kid`; a tampered signature → null | EXECUTED | same | passed |
| 6 | Identity provider: customer from `sub`; staff through the directory with `x-orbit-actor: staff` (role from the directory, name from the token); customer token claiming staff → nobody; no header, `Basic`, no id claim, invalid id, wrong secret → nobody | EXECUTED | same | passed |
| 7 | Mappers: the profile fixture → the masked summary (no raw e-mail in the JSON); verified contact only; unknown `status` → `restricted`, KYC `pending`/`rejected` mapped, demo wallets → `demo`; deposit/withdrawal/operation → records with pt-BR statuses, facts and the id tail; unknown status shown as is; newest first; role map and active filter | EXECUTED | same | passed |
| 8 | Records adapter: available summary/list/record/contact with the service bearer; another kind's reference → `not_found`; 404 → `not_found`; timeout → `timeout`; 503 and a malformed body → `unavailable` | EXECUTED | same | passed |
| 9 | Staff directory: two pages read once, roles mapped, paused member excluded, cache hit, a failed refresh keeps the last listing; a failed first read throws | EXECUTED | same | passed |
| 10 | `maskPhone` on E.164: `+5511999991234` → `+55 ••• ••• 1234`, Mexico, Portugal (3 digits), USA (1 digit), Egypt (20) | EXECUTED | `npm test -w @orbit-support/shared` | 27 tests passed |
| 11 | Every earlier contract unchanged with the simulated adapters (health now also names `staffDirectory`) | EXECUTED | `npm test -w api`, `npm run test:e2e -w api`, `npm test -w web` | 111 passed + 1 skipped; 39; 113 |
| 12 | Static layers and the checker | EXECUTED | `npm run verify` | exit 0 |
| 13 | Full gate on the candidate | EXECUTED | `bash scripts/gate-commit.sh` | the `Gate-Verified` trailer of the commit |
| 14 | CI on the pushed commit | NOT VERIFIED at recording time | `.github/workflows/ci.yml` | recorded in the next state update |

## Limitations
- Nothing ran against the broker's backend: no access exists. The adapters are verified against the frontend's view of the contracts; PH-13.3 runs them against a real environment once BL-032 is answered.
- The id claim order (`sub`, `person_id`, `user_id`, `id`) and the role map are working assumptions marked as such in the code and in DEC-0045.
- `/admin/rastreio/{userId}` is the broker's own support console endpoint; its access control for a service credential is unknown (map §7, Q3).
