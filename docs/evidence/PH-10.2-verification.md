# PH-10.2 — Verification evidence
Type: VERIFICATION EVIDENCE
Work item: PH-10.2 — Formal complaints (DEC-0039 g, DEC-0041)
Recorded on: 2026-09-14
Revision: base `522edbf` plus the PH-10.2 working tree, committed as the next commit on `main` (this record travels with it — §3.4).
Environment: Windows 11 native, Git Bash, Node v24.19.0, npm 11.17.0, Playwright Chromium (headless), Docker Engine 29.8.0 (PostgreSQL 16 test container on 127.0.0.1:55433). Sequential runs; in-memory PGlite for the suites, a scratch PGlite directory for the smoke.

## Claims
| # | Claim | Category | Method / command | Result |
|---|---|---|---|---|
| 1 | `businessDaysAfter`: Wednesday 15:00 São Paulo + 5 → next Wednesday 15:00; Friday + 1 → Monday; a Saturday start counts from Monday; the wall-clock time is kept across Madrid's daylight-saving change | EXECUTED | `npm test -w @orbit-support/shared` (`settings.test.ts`) | 26 tests passed |
| 2 | `staffMayWorkComplaint`: agents false, supervisors and admins true; the customer projection omits `complaintDeadlineAt` | EXECUTED | same (`identity.test.ts`, `cases.test.ts`) | passed (within claim 1) |
| 3 | A customer-chosen complaint gets a deadline 5–9 calendar days after creation, invisible to the customer; its follow-up is a complaint with a deadline | EXECUTED | `npm test -w api` (`cases.service.spec.ts`, PH-10.2) | 95 passed, 1 skipped (PostgreSQL-only) |
| 4 | An agent's take, reply, note, status change, attribute edit and consultation on a complaint → `ForbiddenException('supervisor_required')`, the case stays unassigned; a supervisor takes, replies and consults; the specialist (an agent) answers; handing it to an agent → `BadRequestException`; reclassifying out clears the deadline and reopens the case to agents; reclassifying in sets a fresh one | EXECUTED | same | passed (within claim 3) |
| 5 | Supervision lists open complaints by deadline, counts the ones past it, and drops a resolved one | EXECUTED | same | passed (within claim 3) |
| 6 | Course correction: the first run of claims 3–5 hung — the deadline was computed with a settings query inside an open transaction, which deadlocks PGlite's single connection (5 s test timeout, then 60 s hook timeouts); the operation's time zone is now read before any transaction opens | EXECUTED | first `npm test -w api` run | 3 timeouts → fixed |
| 7 | HTTP: a customer opens a complaint (201, no `complaintDeadlineAt` in the response); an agent's take → 403 `supervisor_required`, reply → 403; the supervisor takes it, the staff detail carries the deadline, the overview's `complaints.list` holds it; handing it to an agent → 400; every earlier contract (strict schemas included) unchanged | EXECUTED | `npm run test:e2e -w api` | 38 tests passed |
| 8 | Migration `0021` adds the enum value and the column; the api suites pass on PostgreSQL 16 with it (the migrator's single transaction accepts `ADD VALUE` as long as the value is not used in the same run — runbook) | EXECUTED | `docker compose -f docker/compose.test.yml -p orbit-pgtest up -d --wait`; `SUPPORT_DATABASE_URL=postgres://postgres:orbit@localhost:55433/orbit_test npm run test:pg -w api`; `down -v` | unit 96/96 (the PostgreSQL-only test included), e2e 38/38; container removed |
| 9 | Web: six topic chips and the complaint note on selection; an agent sees a complaint locked (no take, composer or actions; the supervisor-only note; the deadline); a supervisor sees the "Reclamações formais" section with "1 em aberto · 1 com prazo vencido", opens a complaint from it and sees "Assumir caso" on it | EXECUTED | `npm test -w web` (`NewRequestForm.test.tsx`, `StaffWorkspace.test.tsx`) | 104 tests passed |
| 10 | Browser: the customer opened "Reclamação formal" (SUP-000007) from the chips with the note; the queue showed the tag and deadline; Ana (agent) saw it locked; Carla's supervision listed it with its response deadline; every earlier journey unchanged | OBSERVED | `npm run build`; `SUPPORT_DB_DIR=<scratch> SUPPORT_UPLOADS_DIR=<scratch> node scripts/ui-smoke.mjs <scratch>` | exit 0, 58 observations; `screenshots/ph-10.2/32`, `33` kept (DEC-0034 c) |
| 11 | Static layers and production builds; React purity lint satisfied by keeping the clock in `i18n` (`isPast`) | EXECUTED | `npm run typecheck`, `npm run lint`, `npm run build` | exit 0 (after the `isPast` change) |
| 12 | Full gate on the candidate | EXECUTED | `bash scripts/gate-commit.sh` | the `Gate-Verified` trailer of the commit |
| 13 | CI on the pushed commit | NOT VERIFIED at recording time | `.github/workflows/ci.yml` | recorded in the next state update |

## Gate refusal and fix
The first `scripts/gate-commit.sh` run refused the commit: `OrbitShell.test.tsx` (FND-0011, the first test of its file, untouched by PH-10.2) could not find its first button within Testing Library's 1 s default while the machine was loaded by the preceding api suite (1.9 s); the same suite passed alone twice before and twice after. The first lookup now waits up to 5 s, as FND-0036's test already does; no product code changed.

## Limitations
- Business days ignore public holidays; the deadline is internal (supervision only) and notifies nobody.
- With one supervisor in the simulated directory, "routed to a supervisor" is the supervisors' queue; automatic assignment waits for a real directory (BL-001).
