# PH-10.3 — Verification evidence
Type: VERIFICATION EVIDENCE
Work item: PH-10.3 — Exports on request and recovery to Verification (DEC-0039 h/i, DEC-0042); the PH-10 phase candidate
Recorded on: 2026-09-14
Revision: base `c75526f` plus the PH-10.3 working tree, committed as the next commit on `main` (this record travels with it — §3.4).
Environment: Windows 11 native, Git Bash, Node v24.19.0, npm 11.17.0, Playwright Chromium (headless), Docker Engine 29.8.0 (PostgreSQL 16 test container on 127.0.0.1:55433). Sequential runs; in-memory PGlite for the suites, a scratch PGlite directory for the smoke.

## Claims
| # | Claim | Category | Method / command | Result |
|---|---|---|---|---|
| 1 | The simulated directory has an administrator (`staff-dani`); the earlier role rules unchanged | EXECUTED | `npm test -w @orbit-support/shared` (`identity.test.ts`) | 26 tests passed |
| 2 | `DataExportService`: a supervisor's and an agent's export → `ForbiddenException`; a malformed customer id → `BadRequestException`; the administrator's export is recorded (customer, admin, reason, case count 1) and holds the customer's case with its public messages and events, no internal note, no consultation question, nothing of another customer, and the customer's preference as a boolean; the record lists it and refuses a supervisor; a customer without cases exports an empty, recorded set with the default preference | EXECUTED | `npm test -w api` (`cases.service.spec.ts`, PH-10.3) | 96 passed, 1 skipped (PostgreSQL-only) |
| 3 | Course correction in claim 2: the first assertion expected the default preference for Alice, whom an earlier test in the same file had opted out; the test now asserts the field's type for her and the default for a customer never seen | EXECUTED | first `npm test -w api` run | 1 failure → fixed |
| 4 | Recovery service: a receipt carries `handlingTeam: "verification"` (and only that field more than before); `forwardedTo` is null until forwarded, then `"verification"` | EXECUTED | `npm test -w api` (`access-recovery.service.spec.ts`) | passed (within claim 2) |
| 5 | HTTP: exports by a supervisor and an agent → 403; a short reason → 400; a malformed id → 400; the administrator's → 201 with the record (Dani Alves, reason, one case), the public message only, events, no staff-only field, no internal note, no other customer's text; `GET /api/staff/data-exports` lists it for the admin and refuses a supervisor; a recovery receipt says `handlingTeam: verification`; a request forwarded by staff carries `forwardedTo: verification`; every earlier contract unchanged | EXECUTED | `npm run test:e2e -w api` | 39 tests passed |
| 6 | Migration `0022` creates `data_exports`; the api suites pass on PostgreSQL 16 with migrations `0021`–`0022` | EXECUTED | `docker compose -f docker/compose.test.yml -p orbit-pgtest up -d --wait`; `SUPPORT_DATABASE_URL=postgres://postgres:orbit@localhost:55433/orbit_test npm run test:pg -w api`; `down -v` | unit 97/97, e2e 39/39; container removed |
| 7 | Web: a supervisor's supervision page has no export section; the administrator's has it — "Exportar" stays disabled without a reason, the export posts `{ reason }` with the admin's identity, the status names the case count and customer, and the recorded export appears in the list; the recovery page's forward button reads "Encaminhar à equipe de Verificação" | EXECUTED | `npm test -w web` (`StaffWorkspace.test.tsx`, `AccessRecoveryForm.test.tsx`) | 105 tests passed |
| 8 | Course correction in claim 7: the recovery form test still expected the old "next step" copy; updated to the Verification wording | EXECUTED | first `npm test -w web` run | 1 failure → fixed |
| 9 | Browser, phase candidate: Dani (admin) exported cust-alice's data with a reason — the browser received `orbit-support-cust-alice-<id>.json`, the page confirmed the recorded export and listed it; the recovery page forwarded with the Verification wording; every PH-1..PH-10 journey passed (24/7 availability in the customer's zone, formal complaint locked for an agent and listed for the supervisor, file on the first message, desktop close, …) | OBSERVED | `npm run build`; `SUPPORT_DB_DIR=<scratch> SUPPORT_UPLOADS_DIR=<scratch> node scripts/ui-smoke.mjs docs/evidence/screenshots/ph-10` | exit 0, 59 observations, 34 screenshots (4.5 MB) kept as the phase set (DEC-0034 c) |
| 10 | After that run, one observation string in the smoke that still quoted the previous button label was reworded; no selector or behaviour changed, so the run stands for the final tree | INSPECTED | `git diff scripts/ui-smoke.mjs` | one string |
| 11 | Static layers and production builds | EXECUTED | `npm run typecheck`, `npm run lint`, `npm run build` | exit 0 |
| 12 | Full gate on the candidate | EXECUTED | `bash scripts/gate-commit.sh` | the `Gate-Verified` trailer of the commit |
| 13 | CI on the pushed commit | NOT VERIFIED at recording time | `.github/workflows/ci.yml` | recorded in the next state update |

## Limitations
- The export reaches the administrator's browser as a file; how it reaches the customer is an operating step outside the product.
- The reason is free text; no proof of the customer's request is stored beyond it.
- The Verification hand-off is a labeled simulation until Orbit's process exists (BL-001).
