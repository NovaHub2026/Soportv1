# PH-12 — Project closure
Type: PHASE CONTEXT
Status: ACTIVE
Objective / Feature IDs: all objectives; FEAT-ACCESS, FEAT-NOTIFY, process; hand-over to PH-11
Cycle: 4 (count in the `ROADMAP.md` ledger)

## Outcome and why now
The project is closed except for one phase: PH-11, the AI assistant, which the Owner will build directly with GPT («vamos a dejar la phase del agente de ia para construirla directamente con gpt, vamos a finalizar todas las cosas pendientes para cerrar el proyecto y solo quede esa phase», 2026-09-14). Closing means: every Agent-owned backlog item is done or has a recorded, accepted disposition; the last phases are audited; an internal demo release carries everything delivered; the repository tells the next builder what PH-11 must do and where it plugs in; the state says "closed except PH-11".
Why now: the Owner asked for it; PH-10 is approved and cycle 4 is at 1/3.

## Scope, non-goals and dependencies
In scope: (1) closable debt — BL-027 (likely secrets masked in recovery descriptions), BL-028 (e-mail attempts and a dead-letter state), BL-030 (the checker reads repository paths in shell scripts, Dockerfiles, compose files, `.env.example` and the CI workflow), BL-017 (the unused Nest deploy CLI that carried every dev advisory); (2) a closing audit of PH-10 and this phase by independent reviewers (§8.5 discipline at the requested scope); (3) hand-over — the PH-11 brief for a GPT build (requirements, guardrails, integration points, contracts), the internal demo release v0.2.0-demo with everything since `v0.1.1-demo`, the closure record, the final state.
Non-goals: PH-11 itself; Orbit's adapters (BL-001); retention and the e-mail provider (Owner); a production release (§1.1); toolchain majors (BL-005/006 stay open, low, with their triggers).
Dependencies: PH-10 (approved).

## Product rules and acceptance scenarios
- §4.5 / §10.2: a password or code typed into a recovery description never reaches staff in clear; the original stays only for the verification process.
- RULE-SUP-03 / §4.4: an e-mail that keeps failing stops being retried forever and is visible in the log as given up; the in-product notification stays.
- §7.5: stale repository paths in operational files fail the checker.
- §12.1 / §15: the next builder can start PH-11 from the repository alone.

## Planned subphases, adjusted as evidence arrives
| ID | Block | Status |
|---|---|---|
| PH-12.1 | Closable debt: BL-027, BL-028, BL-030 (remainder), BL-017 — `PH-12.1.md`, approved 2026-09-14 | APPROVED |
| PH-12.2 | Closing audit (independent reviewers) of PH-10 and PH-12.1; remediation | PLANNED |
| PH-12.3 | Hand-over: PH-11 brief, demo release v0.2.0-demo, closure record; phase closure | PLANNED |

## Verification and operational readiness
Per subphase: unit and e2e tests with negative cases; the full smoke on the release candidate; `bash scripts/gate-commit.sh` for every commit; CI green before any tag (DEC-0034 d).

## Completion evidence, findings and context updated
In progress.
