# DECISION LOG
Type: DECISION LOG
Smaller material decisions (`GOVERNANCE.md` §2.3). Durable decisions are ADRs in this directory and are listed here for discoverability.

| ID | Date | Decision | Authority | Status | Related |
|---|---|---|---|---|---|
| ADR-0001 | 2026-09-13 | Adopt GOVERNANCE.md Edition 3.1; project bindings in `CLAUDE.md` | Owner | ACCEPTED | §15.1 |
| ADR-0002 | 2026-09-13 | Dedicated project; explicit Orbit integration boundary; labeled simulation until access exists | Agent (delegated) | ACCEPTED | PH-1, PH-4, BL-001 |
| ADR-0003 | 2026-09-13 | PostgreSQL dialect via Drizzle ORM; embedded PGlite for development/tests; server PostgreSQL for deployment (PH-8) | Agent (delegated) | ACCEPTED | PH-1.2, `ADR-0003-postgresql-drizzle-pglite.md` |
| ADR-0004 | 2026-09-13 | Live updates over Server-Sent Events read with fetch streaming; in-process RxJS bus; resync on every (re)connect; polling kept as a slow safety net | Agent (delegated) | ACCEPTED | PH-2.1, `ADR-0004-sse-live-updates.md` |
| DEC-0001 | 2026-09-13 | Backlog kept in `docs/BACKLOG.md` until an issue tracker is chosen; no competing backlogs | Agent (delegated) | ACCEPTED | §3.1 |
| DEC-0002 | 2026-09-13 | Git author for Agent commits set repo-locally to `NovaHub2026 <orbitmarket.pro@gmail.com>` — no identity existed on the machine; the name is the authenticated GitHub login | Agent; Owner may change | ACCEPTED | `CLAUDE.md` |
| DEC-0003 | 2026-09-13 | No Orbit system exists yet. All Orbit identity/record context is simulated behind the ADR-0002 boundary and labeled as simulation in UI and docs. Owner: «Actualmente no hay ningún repositorio asi que tendremos que hacer todo con datos simulados» | Owner | ACCEPTED | ADR-0002, BL-001, PH-4 |
| DEC-0004 | 2026-09-13 | Workspace tooling: npm workspaces `apps/*`, `packages/*` (npm 11 present, pnpm not installed). Framework scaffolds kept: Next 16 (ESLint, TypeScript 5) and Nest 12 (ESM, TypeScript 6, Vitest, oxlint, Prettier). Vitest + jsdom + Testing Library added to web. Gate profiles context / static / unit / verify / full, with builds outside `verify` to keep the per-commit gate fast. API dev port 3001. Revisit: align TypeScript versions when Next supports TS 6 (BL-005); add API e2e to CI when contracts exist | Agent (delegated) | ACCEPTED | PH-1.1, `../runbooks/VERIFICATION.md` |
| DEC-0005 | 2026-09-13 | Shared contracts live in `packages/shared` (`@orbit-support/shared`): zod schemas + TS types + vocabulary, compiled to `dist/` and built before consumers (`npm run build:shared` inside `verify`). Nest validates request bodies with a small `ZodValidationPipe` instead of class-validator so API and web share one source of truth | Agent (delegated) | ACCEPTED | PH-1.2 |
| DEC-0006 | 2026-09-13 | Web styling with CSS Modules + CSS custom properties (brand tokens in `globals.css`), no Tailwind; browser reaches the API through Next rewrites (`/api/*`, `API_ORIGIN`); conversation refresh by 5 s polling until PH-2; UI evidence produced by a Playwright browser smoke (`scripts/ui-smoke.mjs`, root devDependency) with screenshots under `docs/evidence/screenshots/` | Agent (delegated) | ACCEPTED | PH-1.3 |
| DEC-0007 | 2026-09-13 | Staff workspace copy in pt-BR by default through the same dictionary (staff-facing status labels separate from customer labels); responsible agent displayed by id until staff profiles exist; desktop-first three-column layout, context column hidden below 1100 px. Revisit staff language with the operating team (context §10.1, BL-002) | Agent (delegated) | ACCEPTED | PH-1.4 |
| DEC-0008 | 2026-09-13 | Identity provider is selected by `SUPPORT_IDENTITY_PROVIDER` (only `simulated` exists); the API refuses to start in `NODE_ENV=production` with the simulated provider unless `SUPPORT_ALLOW_SIMULATED_IDENTITY=true` (isolated demo only). `GET /api/identity/me` exposes the actor and its `source` so every client can label simulation | Agent (delegated; protects RULE-SUP-01) | ACCEPTED | PH-1.5, FEAT-ORBIT |
| DEC-0009 | 2026-09-13 | Attachments: upload first (`POST …/attachments`, multipart, multer memory storage), link ids on send; storage behind `AttachmentStorage` with a local-disk adapter (`SUPPORT_UPLOADS_DIR`, default `.data/uploads`) and an in-memory one for tests; allowed type decided by magic bytes (PNG/JPEG/WebP/PDF) in-house, no dependency; downloads served inline with `nosniff`/`no-store` and authorized from the database row, never from the storage key; web fetches bytes with identity headers into blob URLs | Agent (delegated) | ACCEPTED | PH-2.3 |
| DEC-0010 | 2026-09-13 | Resolution = reason code (fixed working list, refine with Operations — BL-002) + customer-facing explanation posted as a public staff message; `resolved`/`closed` never set through the generic status endpoint; PH-3 event types added to the enum in one migration | Agent (delegated) | ACCEPTED | PH-3.1 |
| DEC-0011 | 2026-09-13 | Consultations are first-class rows (`case_consultations`: team from a fixed list, question, requester, status, answer) driving `waiting_internal`; internal notes reuse messages with `internal` visibility and never touch customer-facing activity timestamps; any staff member may answer until roles arrive (PH-7) | Agent (delegated) | ACCEPTED | PH-3.2 |
| DEC-0012 | 2026-09-13 | Reassignment authority: current owner, anyone on an unowned case, or supervisor/admin roles; release = `case_assigned` with `agentId: null`, `released: true`; priority/category edits as `priority_changed` / `category_changed` events; transfer targets from the simulated staff list until a directory exists | Agent (delegated) | ACCEPTED | PH-3.3 |
| DEC-0013 | 2026-09-13 | Closure only from `resolved`, by the in-process `ClosureJob` after the follow-up window (7 days default, env-configurable, `SUPPORT_CLOSURE_JOB=off` to disable) or explicitly by staff, with `closed_reason`; follow-up = new linked case (`parent_case_id`, inherited category, system back-reference, events on both); messages on closed cases stay refused and the UI offers the follow-up form | Agent (delegated) | ACCEPTED | PH-3.4 |
| DEC-0014 | 2026-09-13 | Shared incidents are a simple association (`incidents` + `support_cases.incident_id`): staff link/unlink cases, broadcast one internal note to every linked open case, and mark the incident resolved — which never changes a case status (context §5.4); an internal system-style note tells each linked case's team to confirm the customer's issue individually | Agent (delegated) | ACCEPTED | PH-3.5 |
| DEC-0015 | 2026-09-14 | Customer-facing contracts are explicit projections: `CustomerCaseSummary` / `CustomerCaseDetail` omit `STAFF_ONLY_SUMMARY_FIELDS` (`priority`, `assignedAgentId`, `staffLastReadAt`, `incidentId`, `incidentTitle`) on REST and on customer streams; adding a summary field requires deciding whether customers may see it; free-text fields refuse NUL bytes | Agent (delegated; protects RULE-SUP-04) | ACCEPTED | Cycle Audit 1 FND-0006/0016 |
| DEC-0016 | 2026-09-14 | Idempotency keys: a message key is unique per (case, author type, author id); a creation key is stored on the case, unique per customer, and a key found on a case of the other kind (root vs follow-up) is refused with 409 `client_message_id_reused` instead of answering with a foreign case; migration `0007` backfills creation keys | Agent (delegated) | ACCEPTED | Cycle Audit 1 FND-0010 |
| DEC-0017 | 2026-09-14 | Concurrency rule for the case domain: every change to a case runs inside one transaction that locks and re-reads the row (`CasesService.mutate`, `SELECT … FOR UPDATE`); pre-transaction checks are fast failures only; closure writes nothing unless the locked row is still `resolved`; the closure job is single-flight; transfer targets are validated through a `StaffDirectory` port of the Orbit boundary (simulated list shared with the web) | Agent (delegated) | ACCEPTED | Cycle Audit 1 FND-0007/0009, ADR-0003 revisit (BL-019) |
| DEC-0018 | 2026-09-14 | The root `verify` chain includes the API e2e suite (`npm run test:e2e`), so CI corroborates HTTP contracts; api vitest `hookTimeout` 60 s for cold PGlite starts; production dependency advisories are fixed with root `overrides` when the upstream pin lags (`multer` 2.3.0) | Agent (delegated) | ACCEPTED | Cycle Audit 1 FND-0019/0020 |
| DEC-0019 | 2026-09-14 | Orbit records boundary (PH-4.1): `OrbitRecordsPort` beside the identity port, read-only; every lookup is `{ state: 'available', data }` or `{ state: 'unavailable', reason }` with `source` and `fetchedAt` (RULE-SUP-07 — no zero, no assumed state); contact/destination masking happens inside the adapter so raw values never cross the boundary (§10.2); the context is served by `GET /api/staff/cases/:id/orbit`, separate from the case detail, so an adapter failure never breaks the conversation; `SUPPORT_ORBIT_RECORDS=simulated` (only value) and `SUPPORT_SIMULATED_ORBIT=unavailable` (outage demo) | Agent (delegated) | ACCEPTED | PH-4.1, ADR-0002, DEC-0003 |
| DEC-0020 | 2026-09-14 | Record cards (PH-4.2): one record per case, denormalized on `support_cases` (`record_kind`, `record_reference`, `record_captured_at`, `record_snapshot`, `record_lookup_reason`); the snapshot is captured through `OrbitRecordsPort.getRecord` when the case is created (§6.2) and an unavailable answer still opens the case with the reason on the card (§14 item 7); records are a generic fact list already masked by the adapter, so the customer projection needs no new rule; ownership is part of the lookup (another customer's record is `not_found`); the API never refuses a second case on the same record — the panel suggests continuing the active one (§4.2) | Agent (delegated) | ACCEPTED | PH-4.2, DEC-0019, ADR-0002 |
| DEC-0021 | 2026-09-14 | Staff queue semantics (PH-5.1): seven views (`unassigned`, `mine`, `active`, `waiting_customer`, `waiting_internal`, `resolved`, `closed`); active views order the oldest unanswered customer message first (`awaitingReplySince`, staff-only), waiting views the oldest wait first, history newest first; offset pagination (`limit` ≤ 200) with "Carregar mais" | Agent (delegated) | ACCEPTED | PH-5.1, BL-011 |
| DEC-0022 | 2026-09-14 | Saved replies (PH-5.3): shared team templates in `saved_replies` without placeholders; any staff member creates; editing or removing someone else's needs supervisor/admin (provisional role model until PH-7); inserting a reply only edits the draft — nothing is sent automatically | Agent (delegated) | ACCEPTED | PH-5.3, RULE-SUP-09 |
| DEC-0023 | 2026-09-14 | Operating configuration and honesty (PH-5.4): one attributed `support_settings` row (schedule per weekday + IANA zone, attention threshold, follow-up window) with project working defaults until a supervisor saves; customer availability is computed from it and labeled a working default until then (RULE-SUP-08); service metrics are computed from history for a period and carry `targets: null` (BL-002); overdue = awaiting a human reply longer than the threshold; supervision surfaces are gated by the simulated role until PH-7 | Agent (delegated) | ACCEPTED | PH-5.4, DEC-0021 |
| DEC-0024 | 2026-09-14 | Customer notifications (PH-6.1): rows in `case_notifications` written inside the transaction of the customer-facing change (public staff reply, waiting for customer, resolved, closed) — one per event, none for internal work; they carry only the kind and the case reference; opening the conversation marks the case's notifications read; the host shows a badge fed by the customer-wide stream | Agent (delegated) | ACCEPTED | PH-6.1, RULE-SUP-01/-03/-04 |
| DEC-0025 | 2026-09-14 | E-mail notifications (PH-6.2): notification-only channel through `EmailNotifierPort` (no provider — BL-002); the simulated adapter records `email_outbox` with the masked address, subject, link and no case content; `NotificationJob` e-mails each notification at most once, only while unread and only after `emailDelayMinutes` (default 15), skipping opted-out customers (`customer_preferences`, default on) and customers without a known address; the raw address comes from `OrbitRecordsPort.contactEmail` and never reaches a UI | Agent (delegated) | ACCEPTED | PH-6.2, DEC-0024, §4.4, §10.2 |
| DEC-0026 | 2026-09-14 | Automated notices (PH-6.3): the outside-hours notice is a `system` message with the next opening from the configured schedule, at most one per case per 12 h, plus an `outside_hours` notification; reminders (`reminderAfterHours`, default 48) are one per waiting-for-customer period, recorded as `reminder_sent` events, and never change a case's status (§7.4: silence is not a solution); neither counts as a human response in metrics (§14) | Agent (delegated) | ACCEPTED | PH-6.3, DEC-0023, DEC-0024 |
| DEC-0027 | 2026-09-14 | Cycle Audit 2 rules: (a) the reminder's waiting period starts when a case enters `waiting_customer` or staff write while it waits (`waiting_customer_since`), never at an older staff reply, and each entry resets the reminder marker; (b) outbound e-mails are claim-then-send — a row is marked `emailed_at` before the notifier is called, a failed send releases it, an unavailable Orbit contact lookup stops the batch for that tick (at most once per tick, never lost as "no address"); (c) the time zone is validated with `Intl` at the boundary and availability fails closed on a stored unknown zone; (d) commits are produced only by `scripts/gate-commit.sh` and a `pre-commit` hook runs `check:context` (BL-020 closed); (e) the UI smoke stays a local phase-candidate control — not in CI — until PH-8 decides on a browser container (BL-008 revisit event), while CI builds the applications after `verify`; (f) DEC-0017 exception: the read markers `markCustomerRead`/`markStaffRead` update the case row outside `mutate` (no history, blocked by any concurrent `mutate`) | Agent (delegated) | ACCEPTED | Cycle Audit 2 FND-0030..FND-0033, FND-0040, FND-0045, FND-0046, FND-0052; DEC-0017, DEC-0025, DEC-0026 |
| DEC-0028 | 2026-09-14 | Access recovery route (PH-7.1, §4.5): an unauthenticated `POST /api/public/access-recovery` stores an unverified contact (e-mail or phone) and a description in `access_recovery_requests`, never linked to a customer id or a case; the answer is only a `REC-` reference, the time and the next step ("Orbit's verification process"), identical for known and unknown contacts; retries are idempotent per contact + `clientRequestId`; abuse is bounded by working defaults (3 per contact per hour, 30 per API instance per 10 minutes → 429 with a retry hint); staff record one attributable outcome — "forwarded" is a labeled simulation of the hand-off until Orbit's process exists (BL-002) — or "closed" | Agent (delegated) | ACCEPTED (limits amended: ceiling 120 by DEC-0034 g; per-client limit by DEC-0038 b) | PH-7.1, DEC-0003, RULE-SUP-01 |
| DEC-0029 | 2026-09-14 | Role model (PH-7.2, closes BL-016; §5.1, RULE-SUP-02/-09): one shared table `staffMay(role, action, ownership)` enforced by the API inside the locked transaction and mirrored by the workspace — any staff member may reply (becoming responsible only when the case is unowned), write internal notes, answer consultations and take an unowned case; changing status, resolving, closing, requesting a consultation, editing priority/category, transferring/releasing and linking an incident on a case someone else owns need the owner or a supervisor/admin; supervisors/admins may do everything on every case; incident creation, broadcast and resolution stay team actions. Staff identities resolve only from the directory and the directory's role wins over any header | Agent (delegated) | ACCEPTED | PH-7.2, DEC-0012, Cycle Audit 1 FND-0026 |
| DEC-0030 | 2026-09-14 | Shared-device sign-out (PH-7.3, §10.2): "Sair" on the host and on the staff workspace forgets the simulated identity, removes every `orbit-support.` session buffer and shows a neutral, labeled picker with nothing identity-bound mounted; the host signs out by itself after 30 minutes without activity (working default, `IDLE_SIGN_OUT_MS`) and says so; the staff workspace has no idle timeout (staff protection follows Orbit's policies). Real sessions replace the simulated stores without changing the shells' contract (BL-001) | Agent (delegated) | ACCEPTED | PH-7.3, DEC-0003, RULE-SUP-01 |
| DEC-0031 | 2026-09-14 | Hardening working defaults (PH-8.1, BL-012): helmet's default headers and no `X-Powered-By` on every response; JSON 404 for every unknown path; the API binds to loopback unless `SUPPORT_BIND` says otherwise and warns when bound to a network without `NODE_ENV=production`; one identity may hold at most `SUPPORT_SSE_MAX_PER_IDENTITY` (8) open streams and start 30 uploads per 10 minutes (`ATTACHMENT_LIMITS.maxUploadsPer10Minutes`) per API instance — refused with 429 and a retry hint; limits are in-memory per instance until PH-8.3 decides the topology | Agent (delegated) | ACCEPTED | PH-8.1, Cycle Audit 1 FND-0018/FND-0025, DEC-0008 |
| DEC-0032 | 2026-09-14 | ADR-0003 revisit (PH-8.2, BL-019): PGlite remains the development and fast-test database; a PostgreSQL server reached through `SUPPORT_DATABASE_URL` (`pg` pool, `drizzle-orm/node-postgres`, same migrations) is the deployment database and the second verification path — `npm run test:pg -w api` locally against `docker/compose.test.yml` and the CI job `verify-postgres` on every push; services depend only on the common `PgDatabase` type | Agent (delegated) | ACCEPTED | PH-8.2, ADR-0003, Cycle Audit 1 FND-0009 |
| DEC-0033 | 2026-09-14 | Deployment shape (PH-8.3): one API instance (in-process event bus, in-memory limits, background jobs), a PostgreSQL server (`SUPPORT_DATABASE_URL`), the web in front of it with the `/api` rewrite and only the web port published; TLS, domain, hosting, secrets and a mail provider are Owner decisions (§1.1); a public deployment never carries `SUPPORT_ALLOW_SIMULATED_IDENTITY` — until Orbit's identity exists the honest release is an access-restricted demo (`docs/runbooks/RELEASE.md`) | Agent (delegated) | ACCEPTED | PH-8.3, DEC-0008, DEC-0031, DEC-0032, BL-001 |
| DEC-0034 | 2026-09-14 | Cycle 3 out-of-band audit rules: (a) the web image bakes `API_ORIGIN` at build time (Next rewrites), compose publishes only the web and only on 127.0.0.1, the demo switch defaults to false and any non-loopback API bind is announced whatever `NODE_ENV` says; (b) the api suites refuse any `SUPPORT_DATABASE_URL` whose database is not named `*_test`; (c) the gate stages tracked changes plus untracked paths named with `--include` and refuses a tree that changed during `verify`; (d) a release is tagged only after its commit's CI is green on both jobs, and the release gate checks that no carried item is past its revisit event; (e) staff display names come from the directory, never a header; "take" claims an unowned case for every role; (f) full screenshot sets are committed only at phase and release candidates — subphase runs keep only their new observations; (g) recovery contacts are normalized for limits and idempotency, recovery text refuses control/bidi characters, and the instance-wide ceiling (120 per 10 minutes) answers `service_busy`; (h) PostgreSQL migrations run under a session advisory lock; e-mail delivery is at most once (restating DEC-0027 b) | Agent (delegated) | ACCEPTED | cycle 3 out-of-band audit FND-0058..FND-0081; DEC-0008, DEC-0027, DEC-0029, DEC-0031..DEC-0033 |
| DEC-0035 | 2026-09-14 | Case-domain rules (PH-9.1): (a) waiting for an internal team = status `waiting_internal` or any open consultation, dated by the older of the waiting period and the oldest open consultation — moving a case on does not hide a team's pending answer (BL-029, FND-0078); (b) the supervision overdue list holds each case once, at its oldest wait; (c) internal notes take an optional retry key in the reply key space (per case and author); a key already used for a public reply is refused as a note (409 `client_message_id_reused`) instead of returning a message of the other visibility (BL-013); (d) rules read by several services live in `apps/api/src/cases/case-rules.ts`, and reminders are decided by `CasesService.remind` under the case lock (DEC-0017, BL-022) | Agent (delegated) | ACCEPTED | PH-9.1 |
| DEC-0036 | 2026-09-14 | Staff workspace rules (PH-9.2): (a) system messages carry what they say — `case_messages.system_kind` (`follow_up_of`, `outside_hours`) and `system_data` (migration `0019`) — and every client words them from its dictionary; `body` keeps the pt-BR text as written and is shown only when no kind is set (older rows, unknown kinds) (BL-024); (b) the staff composer keeps one retry key per mode (reply, note) until that send succeeds; a customer's pending or failed message lists the files it carries (BL-013); (c) the queue views are real tabs with automatic activation (one tab stop, arrows/Home/End, a labelled tab panel); (d) settings number fields keep the typed text and an empty or non-integer value is refused on the client by its label, ranges stay the API's (BL-024); (e) every staff case action goes through one `runAction` in `StaffCaseView`, whose parts live in `CaseActions`, `StaffComposer`, `StaffMessage` and `CaseContext` (BL-014) | Agent (delegated) | ACCEPTED | PH-9.2 |
| DEC-0037 | 2026-09-14 | Customer panel rules (PH-9.3): (a) a customer may upload files before the case exists — `POST /api/support/attachments`, same type/size/rate checks, `case_attachments.case_id` null until linked (migration `0020`), storage key `staged/<id>`; `createCaseSchema.attachmentIds` links the creator's own unattached staged files to the first message inside the creation transaction, any other id fails the whole creation (400 `attachment_not_available`); a staged file is never downloadable (downloads are authorized per case) and the follow-up form stays text-only (BL-010); (b) on desktop the side panel is shown unless the customer closed it: "×" and the topbar "Fechar suporte" hide it, the topbar "Suporte" shows it again, focus returns to that button, and a hidden panel marks nothing read; mobile keeps its full-screen toggle (BL-015) | Agent (delegated) | ACCEPTED | PH-9.3 |
| DEC-0038 | 2026-09-14 | Operations and process rules (PH-9.4): (a) every 429 carries `Retry-After` — the body's `retryAfterSeconds`, or 5 s when the refusal has none (global `RetryAfterFilter`); (b) the recovery route also limits each client address, 10 per 10 minutes, 429 `too_many_from_client` with its own copy — only where a reverse proxy that appends `X-Forwarded-For` fronts the web and the deployment says so with `SUPPORT_TRUST_PROXY` (number of such proxies, usually 1; unset by default). The web's own proxy (Next rewrites) neither adds the browser's address nor drops a forged one (observed in PH-9.4), so without such a proxy no address identifies a client and the limit stays off; the per-contact limit and the instance ceiling always apply (BL-026); (c) uploads never linked to a message are removed after `SUPPORT_UNLINKED_ATTACHMENT_GRACE_HOURS` (24) by an hourly job, row first and only while still unlinked, then the bytes; linking checks the update count so a file removed meanwhile fails the send (BL-009); (d) the smoke reads every case reference from the product (BL-023); (e) `check-context` also checks paths inside command spans, commit hashes and release tags cited by live documents (the CI `verify` job checks out full history), reads the ledger record as its first `.md` path and refuses an out-of-band record for a due cycle (BL-030) | Agent (delegated) | ACCEPTED | PH-9.4 |
| DEC-0039 | 2026-09-14 | Operating policies decided by the Owner (BL-002; «vamos a decidir las politicas ya», answered question by question): (a) the human support team works 24/7; internal times are America/Sao_Paulo and every customer sees them in their own time zone; (b) the first line will be an AI assistant (future phase; the Owner named ChatGPT) — RULE-SUP-10 is kept: a customer who asks for a person is transferred, and disputes and security issues always go to a person; (c) attention threshold 4 h, follow-up window 7 days, e-mail after 15 min unread, one reminder after 48 h waiting for the customer; (d) no first-response target yet; (e) consultation teams stay finance, operations, security, verification, product; (f) retention of closed cases pending legal advice — nothing is deleted except uploads never linked to a message (24 h, DEC-0038 c); (g) formal complaints: a topic the customer can choose ("Reclamação formal"), routed to a supervisor, internal deadline 5 business days, recorded as such; staff may reclassify; (h) exports of a customer's support data: an admin only, on the customer's request, recorded; (i) account recovery: requests are forwarded to the Verification team, which uses Orbit's KYC process; (j) e-mail provider: decided later (the simulated outbox stays) | Owner | ACCEPTED | BL-002, `../runbooks/OPERATIONS.md`, PH-10 |
| DEC-0040 | 2026-09-14 | Schedule representation for DEC-0039 a (PH-10.1): the default settings are the Owner's decided policies (24/7 in America/Sao_Paulo) — the customer copy no longer calls them a working default; "24:00" is a valid closing time so a day can be open around the clock (never an opening time); `Availability` carries `alwaysOpen`, `todayWindow` and `nextOpeningAt` as instants and the outside-hours notice stores `nextOpeningAt`, so every client formats them in the customer's own zone — the browser's until Orbit's session carries one (BL-001); a 24/7 schedule announces no next opening | Agent (delegated) | ACCEPTED | PH-10.1 |
| DEC-0041 | 2026-09-14 | Formal complaints for DEC-0039 g (PH-10.2): a case category `formal_complaint` the customer chooses (staff may reclassify in either direction, the `category_changed` event kept); `support_cases.complaint_deadline_at` = five business days (Monday–Friday of the operation's zone, no holiday calendar, same wall-clock time) after the case became a complaint, inherited by a follow-up, cleared on reclassification out; staff-only in the contract; `staffMayWorkComplaint(role)`: agents may not take, reply, note, change state, consult, edit or transfer a complaint (403 `supervisor_required`) and a complaint cannot be handed to an agent (400) — answering a consultation stays open to the specialist; supervision lists open complaints by deadline and counts the ones past it; "routing to a supervisor" is the supervisors' queue, not an automatic assignment (the directory has one supervisor); the deadline is internal — not announced to the customer, no notification | Agent (delegated) | ACCEPTED | PH-10.2 |
