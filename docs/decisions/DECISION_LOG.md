# DECISION LOG
Type: DECISION LOG
Smaller material decisions (`GOVERNANCE.md` §2.3). Durable decisions are ADRs in this directory and are listed here for discoverability.

| ID | Date | Decision | Authority | Status | Related |
|---|---|---|---|---|---|
| ADR-0001 | 2026-09-13 | Adopt GOVERNANCE.md Edition 3.1; project bindings in `CLAUDE.md` | Owner | ACCEPTED | §15.1 |
| ADR-0002 | 2026-09-13 | Dedicated project; explicit Orbit integration boundary; labeled simulation until access exists | Agent (delegated) | ACCEPTED | PH-1, PH-4, BL-001 |
| DEC-0001 | 2026-09-13 | Backlog kept in `docs/BACKLOG.md` until an issue tracker is chosen; no competing backlogs | Agent (delegated) | ACCEPTED | §3.1 |
| DEC-0002 | 2026-09-13 | Git author for Agent commits set repo-locally to `NovaHub2026 <orbitmarket.pro@gmail.com>` — no identity existed on the machine; the name is the authenticated GitHub login | Agent; Owner may change | ACCEPTED | `CLAUDE.md` |
| DEC-0003 | 2026-09-13 | No Orbit system exists yet. All Orbit identity/record context is simulated behind the ADR-0002 boundary and labeled as simulation in UI and docs. Owner: «Actualmente no hay ningún repositorio asi que tendremos que hacer todo con datos simulados» | Owner | ACCEPTED | ADR-0002, BL-001, PH-4 |
| ADR-0003 | 2026-09-13 | PostgreSQL dialect via Drizzle ORM; embedded PGlite for development/tests; server PostgreSQL for deployment (PH-8) | Agent (delegated) | ACCEPTED | PH-1.2, `ADR-0003-postgresql-drizzle-pglite.md` |
| DEC-0005 | 2026-09-13 | Shared contracts live in `packages/shared` (`@orbit-support/shared`): zod schemas + TS types + vocabulary, compiled to `dist/` and built before consumers (`npm run build:shared` inside `verify`). Nest validates request bodies with a small `ZodValidationPipe` instead of class-validator so API and web share one source of truth | Agent (delegated) | ACCEPTED | PH-1.2 |
| DEC-0023 | 2026-09-14 | Operating configuration and honesty (PH-5.4): one attributed `support_settings` row (schedule per weekday + IANA zone, attention threshold, follow-up window) with project working defaults until a supervisor saves; customer availability is computed from it and labeled a working default until then (RULE-SUP-08); service metrics are computed from history for a period and carry `targets: null` (BL-002); overdue = awaiting a human reply longer than the threshold; supervision surfaces are gated by the simulated role until PH-7 | Agent (delegated) | ACCEPTED | PH-5.4, DEC-0021 |
| DEC-0022 | 2026-09-14 | Saved replies (PH-5.3): shared team templates in `saved_replies` without placeholders; any staff member creates; editing or removing someone else's needs supervisor/admin (provisional role model until PH-7); inserting a reply only edits the draft — nothing is sent automatically | Agent (delegated) | ACCEPTED | PH-5.3, RULE-SUP-09 |
| DEC-0021 | 2026-09-14 | Staff queue semantics (PH-5.1): seven views (`unassigned`, `mine`, `active`, `waiting_customer`, `waiting_internal`, `resolved`, `closed`); active views order the oldest unanswered customer message first (`awaitingReplySince`, staff-only), waiting views the oldest wait first, history newest first; offset pagination (`limit` ≤ 200) with "Carregar mais" | Agent (delegated) | ACCEPTED | PH-5.1, BL-011 |
| DEC-0020 | 2026-09-14 | Record cards (PH-4.2): one record per case, denormalized on `support_cases` (`record_kind`, `record_reference`, `record_captured_at`, `record_snapshot`, `record_lookup_reason`); the snapshot is captured through `OrbitRecordsPort.getRecord` when the case is created (§6.2) and an unavailable answer still opens the case with the reason on the card (§14 item 7); records are a generic fact list already masked by the adapter, so the customer projection needs no new rule; ownership is part of the lookup (another customer's record is `not_found`); the API never refuses a second case on the same record — the panel suggests continuing the active one (§4.2) | Agent (delegated) | ACCEPTED | PH-4.2, DEC-0019, ADR-0002 |
| DEC-0019 | 2026-09-14 | Orbit records boundary (PH-4.1): `OrbitRecordsPort` beside the identity port, read-only; every lookup is `{ state: 'available', data }` or `{ state: 'unavailable', reason }` with `source` and `fetchedAt` (RULE-SUP-07 — no zero, no assumed state); contact/destination masking happens inside the adapter so raw values never cross the boundary (§10.2); the context is served by `GET /api/staff/cases/:id/orbit`, separate from the case detail, so an adapter failure never breaks the conversation; `SUPPORT_ORBIT_RECORDS=simulated` (only value) and `SUPPORT_SIMULATED_ORBIT=unavailable` (outage demo) | Agent (delegated) | ACCEPTED | PH-4.1, ADR-0002, DEC-0003 |
| DEC-0018 | 2026-09-14 | The root `verify` chain includes the API e2e suite (`npm run test:e2e`), so CI corroborates HTTP contracts; api vitest `hookTimeout` 60 s for cold PGlite starts; production dependency advisories are fixed with root `overrides` when the upstream pin lags (`multer` 2.3.0) | Agent (delegated) | ACCEPTED | Cycle Audit 1 FND-0019/0020 |
| DEC-0017 | 2026-09-14 | Concurrency rule for the case domain: every change to a case runs inside one transaction that locks and re-reads the row (`CasesService.mutate`, `SELECT … FOR UPDATE`); pre-transaction checks are fast failures only; closure writes nothing unless the locked row is still `resolved`; the closure job is single-flight; transfer targets are validated through a `StaffDirectory` port of the Orbit boundary (simulated list shared with the web) | Agent (delegated) | ACCEPTED | Cycle Audit 1 FND-0007/0009, ADR-0003 revisit (BL-019) |
| DEC-0016 | 2026-09-14 | Idempotency keys: a message key is unique per (case, author type, author id); a creation key is stored on the case, unique per customer, and a key found on a case of the other kind (root vs follow-up) is refused with 409 `client_message_id_reused` instead of answering with a foreign case; migration `0007` backfills creation keys | Agent (delegated) | ACCEPTED | Cycle Audit 1 FND-0010 |
| DEC-0015 | 2026-09-14 | Customer-facing contracts are explicit projections: `CustomerCaseSummary` / `CustomerCaseDetail` omit `STAFF_ONLY_SUMMARY_FIELDS` (`priority`, `assignedAgentId`, `staffLastReadAt`, `incidentId`, `incidentTitle`) on REST and on customer streams; adding a summary field requires deciding whether customers may see it; free-text fields refuse NUL bytes | Agent (delegated; protects RULE-SUP-04) | ACCEPTED | Cycle Audit 1 FND-0006/0016 |
| DEC-0014 | 2026-09-13 | Shared incidents are a simple association (`incidents` + `support_cases.incident_id`): staff link/unlink cases, broadcast one internal note to every linked open case, and mark the incident resolved — which never changes a case status (context §5.4); an internal system-style note tells each linked case's team to confirm the customer's issue individually | Agent (delegated) | ACCEPTED | PH-3.5 |
| DEC-0013 | 2026-09-13 | Closure only from `resolved`, by the in-process `ClosureJob` after the follow-up window (7 days default, env-configurable, `SUPPORT_CLOSURE_JOB=off` to disable) or explicitly by staff, with `closed_reason`; follow-up = new linked case (`parent_case_id`, inherited category, system back-reference, events on both); messages on closed cases stay refused and the UI offers the follow-up form | Agent (delegated) | ACCEPTED | PH-3.4 |
| DEC-0012 | 2026-09-13 | Reassignment authority: current owner, anyone on an unowned case, or supervisor/admin roles; release = `case_assigned` with `agentId: null`, `released: true`; priority/category edits as `priority_changed` / `category_changed` events; transfer targets from the simulated staff list until a directory exists | Agent (delegated) | ACCEPTED | PH-3.3 |
| DEC-0011 | 2026-09-13 | Consultations are first-class rows (`case_consultations`: team from a fixed list, question, requester, status, answer) driving `waiting_internal`; internal notes reuse messages with `internal` visibility and never touch customer-facing activity timestamps; any staff member may answer until roles arrive (PH-7) | Agent (delegated) | ACCEPTED | PH-3.2 |
| DEC-0010 | 2026-09-13 | Resolution = reason code (fixed working list, refine with Operations — BL-002) + customer-facing explanation posted as a public staff message; `resolved`/`closed` never set through the generic status endpoint; PH-3 event types added to the enum in one migration | Agent (delegated) | ACCEPTED | PH-3.1 |
| DEC-0009 | 2026-09-13 | Attachments: upload first (`POST …/attachments`, multipart, multer memory storage), link ids on send; storage behind `AttachmentStorage` with a local-disk adapter (`SUPPORT_UPLOADS_DIR`, default `.data/uploads`) and an in-memory one for tests; allowed type decided by magic bytes (PNG/JPEG/WebP/PDF) in-house, no dependency; downloads served inline with `nosniff`/`no-store` and authorized from the database row, never from the storage key; web fetches bytes with identity headers into blob URLs | Agent (delegated) | ACCEPTED | PH-2.3 |
| ADR-0004 | 2026-09-13 | Live updates over Server-Sent Events read with fetch streaming; in-process RxJS bus; resync on every (re)connect; polling kept as a slow safety net | Agent (delegated) | ACCEPTED | PH-2.1, `ADR-0004-sse-live-updates.md` |
| DEC-0008 | 2026-09-13 | Identity provider is selected by `SUPPORT_IDENTITY_PROVIDER` (only `simulated` exists); the API refuses to start in `NODE_ENV=production` with the simulated provider unless `SUPPORT_ALLOW_SIMULATED_IDENTITY=true` (isolated demo only). `GET /api/identity/me` exposes the actor and its `source` so every client can label simulation | Agent (delegated; protects RULE-SUP-01) | ACCEPTED | PH-1.5, FEAT-ORBIT |
| DEC-0007 | 2026-09-13 | Staff workspace copy in pt-BR by default through the same dictionary (staff-facing status labels separate from customer labels); responsible agent displayed by id until staff profiles exist; desktop-first three-column layout, context column hidden below 1100 px. Revisit staff language with the operating team (context §10.1, BL-002) | Agent (delegated) | ACCEPTED | PH-1.4 |
| DEC-0006 | 2026-09-13 | Web styling with CSS Modules + CSS custom properties (brand tokens in `globals.css`), no Tailwind; browser reaches the API through Next rewrites (`/api/*`, `API_ORIGIN`); conversation refresh by 5 s polling until PH-2; UI evidence produced by a Playwright browser smoke (`scripts/ui-smoke.mjs`, root devDependency) with screenshots under `docs/evidence/screenshots/` | Agent (delegated) | ACCEPTED | PH-1.3 |
| DEC-0004 | 2026-09-13 | Workspace tooling: npm workspaces `apps/*`, `packages/*` (npm 11 present, pnpm not installed). Framework scaffolds kept: Next 16 (ESLint, TypeScript 5) and Nest 12 (ESM, TypeScript 6, Vitest, oxlint, Prettier). Vitest + jsdom + Testing Library added to web. Gate profiles context / static / unit / verify / full, with builds outside `verify` to keep the per-commit gate fast. API dev port 3001. Revisit: align TypeScript versions when Next supports TS 6 (BL-005); add API e2e to CI when contracts exist | Agent (delegated) | ACCEPTED | PH-1.1, `../runbooks/VERIFICATION.md` |
