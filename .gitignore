# dependencies
node_modules/

# build outputs
dist/
.next/
out/
coverage/
*.tsbuildinfo

# local development data (embedded database files)
.data/

# ad-hoc UI smoke output (evidence runs are written to a named folder per work item)
docs/evidence/screenshots/latest/

# environment (never commit secrets — GOVERNANCE.md §11)
.env
.env.*
!.env.example

# misc
*.log
.DS_Store
# gate-then-commit output (scripts/gate-commit.sh)
.gate-verify.log
