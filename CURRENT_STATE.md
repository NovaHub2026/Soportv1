# CURRENT STATE
Type: CURRENT STATE
Synchronized on: 2026-09-14
Derived from: `docs/phases/ROADMAP.md`, `docs/phases/PH-13.md`; base checkpoint: the PH-13.2 commit (after `cbcd3b4`)

| Field | Value |
|---|---|
| Active objective / feature | PH-13 — Orbit integration readiness (Owner, 2026-09-14): the broker's frontend analysed read-only, Orbit Support prepared to connect; the Owner's answers of DEC-0046 applied (token proven by the broker, a service account as the credential, admins and super admins supervise, the panel inside the broker's app). PH-11 stays the Owner's GPT build. Internal loopback demo `v0.2.0-demo`. |
| Active phase / subphase | PH-13 `ACTIVE`; PH-13.1 `APPROVED` (map, adapters, DEC-0045); PH-13.2 `APPROVED` (verification by the broker, service session, role map, `/embed` hand-off, host component proposal — DEC-0046); next PH-13.3 (live connection) `PLANNED`, waiting for the Owner's service account and dev access (BL-032, narrowed). PH-11 `PLANNED` (Owner). |
| Audit | Cycle 1: 3/3 — CLOSED. Cycle 2: 3/3 — CLOSED. Cycle 3: 3/3 — CLOSED 2026-09-14. Cycle 4: 2/3 (PH-10, PH-12); the closing audit `docs/audits/CLOSING.md` was out of band (closed). The next first-time phase approval (PH-13 or PH-11) makes Cycle Audit 4 due. |
| Blocking decisions / dependencies | Owner: create the support service account in the broker's back-office (team member "Orbit Support", role `auditor`, active, no 2FA) and hand its e-mail/password to the API's environment (`ORBIT_SERVICE_EMAIL` / `ORBIT_SERVICE_PASSWORD`); grant the dev environment (`ORBIT_API_BASE_URL`); mount the proposed host component in the broker's repository (`docs/integration/broker-host/OrbitSupportPanel.tsx`) and allow the panel's origin at the edge (`frame-ancestors`); push the demo tag. Then retention, the e-mail provider (BL-002) and the AI provider account (BL-031). A production release needs the live connection (PH-13.3), those policies and the Owner's authorization (§1.1). |
| Integration / CI / release | CI's `verify` job fails on every push with "tag v0.2.0-demo does not exist in this clone" until the Owner pushes the tag (`git push origin v0.2.0-demo`); the PostgreSQL job passes. Last commits: `cbcd3b4` (PH-13.1), the PH-13.2 commit (CI pending at recording time). No production release authorized. |
| Environment (observed 2026-09-14) | Windows 11 native, Git Bash / PowerShell, Node v24.19.0, npm 11.17.0, Docker Engine 29.8.0 (test PostgreSQL on 127.0.0.1:55433). Fresh clone: set the git author (DEC-0002) and `git config core.hooksPath scripts/git-hooks`. The broker's repository `C:\Proyectos\optaqode-frontend2.0` is read-only for this project. |
| Context route | `CONTEXT_INDEX.md` → six live feature contexts + the integration map; audit records `docs/audits/`; commands in `docs/runbooks/VERIFICATION.md`; policies in `docs/runbooks/OPERATIONS.md`; adapters and the service account in `docs/runbooks/DEPLOYMENT.md` "Orbit adapters" |

## Next valid action
Action: PH-13.3 — the live connection, once the Owner provides the service account and the dev environment: run the API with `SUPPORT_IDENTITY_PROVIDER=optaqode`, `SUPPORT_ORBIT_RECORDS=optaqode` and the `ORBIT_*` variables against `https://dev-api.orbitmarket.pro/api/v1`, record a contract run (login and refresh of the service account, `/admin/rastreio` for a known customer, `/admin/team-members`, a customer token through `/profiles/me`), fix what the real shapes disagree with, confirm the enumerations and protocol numbers, mount the host component in the broker (Owner's PR), run a smoke of the embedded panel, and decide the staff session. Until then: nothing for the Agent.
Why now: PH-13.1 and PH-13.2 left the API and the panel connectable; only real access is missing.
Preconditions: `ORBIT_SERVICE_EMAIL` / `ORBIT_SERVICE_PASSWORD` (or `ORBIT_SERVICE_TOKEN`) and `ORBIT_API_BASE_URL` for the dev environment, given by the Owner out of band (never committed — §11); the tag pushed so CI corroborates the last commits.
Evidence/read first: `docs/phases/PH-13.md`, `docs/integration/ORBIT-INTEGRATION.md` §6–§8, `docs/runbooks/DEPLOYMENT.md` "Orbit adapters", `docs/evidence/PH-13.2-verification.md`.
If preconditions fail: the service account answers a 2FA challenge → disable 2FA on it or use a static token; `/admin/rastreio` refuses the `auditor` role → the Owner raises the account's permissions (`users.view`) or role; the broker's shapes differ → adjust the mappers with the real fixtures and re-run the suites.
